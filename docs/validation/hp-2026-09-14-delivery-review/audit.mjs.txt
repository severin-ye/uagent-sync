import fs from 'node:fs';import path from 'node:path';import {execFileSync} from 'node:child_process';import {createHash} from 'node:crypto';
const root='<USER_HOME>/hpb/src',base='<USER_HOME>/hpb';process.chdir(root);
const old=JSON.parse(execFileSync('git',['show','38c3dbb:package-lock.json'])),now=JSON.parse(fs.readFileSync('package-lock.json'));
const changes=[];for(const k of new Set([...Object.keys(old.packages),...Object.keys(now.packages)])){if(!k)continue;const a=old.packages[k],b=now.packages[k];if(JSON.stringify(a)!==JSON.stringify(b))changes.push({package:k,versionBefore:a?.version,versionAfter:b?.version,changedKeys:[...new Set([...Object.keys(a||{}),...Object.keys(b||{})])].filter(x=>JSON.stringify(a?.[x])!==JSON.stringify(b?.[x]))});}
const ancestors=[];for(const label of ['full-current','full-node18']){let p='<ISOLATED_PUBLIC>/e2/env-'+label+'/tmp';while(true){ancestors.push({label,path:p.replace('<ISOLATED_PUBLIC>','<ISOLATED_PUBLIC>'),plugin:fs.existsSync(p+'/node_modules/@opencode-ai/plugin'),sdk:fs.existsSync(p+'/node_modules/@opencode-ai/sdk')});const next=path.dirname(p);if(next===p)break;p=next;}}
const evidence='<USER_HOME>/<WORKSPACE>/uagent-validation-hp-20260911-173421';
const names=['protected-state-before.json','protected-state-after.json','protected-state-comparison.json','protected-state-detail.json'];
const memory=names.map(n=>{const p=evidence+'/'+n;if(!fs.existsSync(p))return {name:n,exists:false};const b=fs.readFileSync(p);const obj=JSON.parse(b.toString().replace(/^\uFEFF/,''));return {name:n,exists:true,sha256:createHash('sha256').update(b).digest('hex'),topLevelKeys:Object.keys(obj)};});
const snap='<USER_HOME>/AppData/Local/Temp/uagent-hp-revalidation-evidence-d076fff/hp-profile-no-skills/manifest.json';let backup={exists:fs.existsSync(snap)};if(backup.exists){const o=JSON.parse(fs.readFileSync(snap,'utf8').replace(/^\uFEFF/,''));backup.topLevelKeys=Object.keys(o);backup.entries=JSON.stringify(o).includes('package-lock.json');backup.includesUserPackageManifest=JSON.stringify(o).includes('package.json');}
const logs=['s2-consumer-install.json','s2-consumer-install.log','s2-host-install.json','s2-host-install.log'];const incident=logs.map(n=>{const p='<USER_HOME>/m2ise/'+n;return{name:n,exists:fs.existsSync(p),sha256:fs.existsSync(p)?createHash('sha256').update(fs.readFileSync(p)).digest('hex'):null};});
const result={changes,versionsUnchanged:changes.every(c=>c.versionBefore===c.versionAfter),onlyDevFlagsChanged:changes.every(c=>JSON.stringify(c.changedKeys)==='["dev"]'),ancestors,memory,knownSnapshotMetadata:backup,incident,engines:Object.entries(now.packages).filter(([k,v])=>k&&!v.dev&&v.engines?.node).map(([k,v])=>({package:k,node:v.engines.node})),scannerDiff:execFileSync('git',['diff','38c3dbb','ce6780f','--','src/lib/profile*']).toString()};fs.writeFileSync(base+'/audit.json',JSON.stringify(result,null,2));console.log(JSON.stringify({changes:changes.length,versionsUnchanged:result.versionsUnchanged,onlyDevFlagsChanged:result.onlyDevFlagsChanged,ancestorPollution:ancestors.some(a=>a.plugin||a.sdk),memory,backup}));
