# 惠普集中独立验收与本机事件核对

固定源码：`ce6780faa0995c3c3587658aa46a161b4b5e4bcf`；比较基线：`38c3dbbc0e2c5c50654c62aee77550b16181d0c2`；接收交接：`8d203a0e20542b011575a9c3e36141455efc4e50`。独立短目录固定源码；测试期间仅快进读取后续文档，生产/测试/依赖字节未改变。只拥有本报告与惠普清单行，不修改生产或荣耀状态。

## 已执行

当前Node v24.19.0与Node18.20.8完整npm test分别855/855、855/855，退出码0/0；构建、M2、路径及三项真实打包消费者均包含于全量，不重复同一版本定向。完整日志及实际命令见logs/；隔离依赖ci明确prefix，退出0。

消费者使用Public短路径，避开旧用户node_modules；每个运行时的TMP祖先至盘根均核对plugin/SDK不存在。fixture自身先private manifest，再明确prefix安装；新运行依赖自身携带SDK属于预期。HOME/USERPROFILE、应用、Codex、TMP/TEMP/TMPDIR、npm配置/cache/prefix均隔离；两顶层工作区变量、NODE_PATH/NODE_OPTIONS被删除，PATH明确当前运行时优先。pre-env保留执行前证据。只调用人工cfg对象，不注册真实宿主。

路径审阅：三个生产位置均由dirname(fileURLToPath(import.meta.url))取得模块所在目录，继续使用原有相对层级；file URL解码与Windows盘符交给Node标准转换，不依赖cwd。dashboard资产表/目录检查、MCP工作区优先顺序、插件skills去重保持。扫描器差异为空。

lock审阅：包集合/版本不变，32处条目只移除dev标记；根plugin从开发范围^1.18.15改为生产精确1.18.15，SDK仍锁定1.18.15。这会把其传递依赖带入omit-dev，是入口所需，不声称依赖树没有增加运行时安装体积。

## 一批发现与限制

HP-BATCH-01：原run-batch.ps1在本机Get-Command node返回多个Application时将路径数组当成一个命令，先于ci失败。wrapper-red记录环境故障，不当安全红测；改用batch.mjs显式C:/node.exe与指定Node18驱动原replay.mjs，完成两全量。未修改交付源码/包装脚本。接收端若维护通用包装器，应接受明确Node路径或选择单一解析结果并补多Node回归，无需重跑未变生产。

HP-BATCH-02：运行依赖toml声明Node>=20。Node18本次入口与全量行为成功，不等于engine-strict安装或其所有API获支持；未配置engine-strict重跑消费者，不擅自升级/降级依赖。这是兼容声明限制，单列而非抹去实测通过。

## 惠普npm旧事件与历史记忆

已核对本机原始s2-consumer-install、s2-host-install的命令和日志：空cwd无prefix，分别added7/removed36与added27，均退出0。现有事件报告明确涉及用户根package.json、lock与node_modules，未保存三者事前完整状态；本轮仅看现有记录、已知备份manifest及用户根package*文件元数据，未读取/修改这些真实包正文。

已知9月11日config/rules/memories快照manifest存在，但不含用户根package.json/package-lock.json，不能覆盖9月13日npm事件。已知事件证据目录未见事前备份记录；这是有界核对，不能断言整台机器绝无其他备份。最小缺失输入是事前这两个包文件及依赖树/可重建锁定状态的可靠备份位置；若无法提供，则须用户选择希望保留的目标依赖状态，另行授权修复。不能把后续隔离成功当旧现场已恢复。

HP-05已有四份原始前后/差异记录仍在，只读核对显示25条变化（Git元数据21、其他4）；无实际写入者证据，无事前全文备份。未读取真实记忆正文或操作其Git。历史变化继续来源未知；最小缺口为对应时段可归因写入日志或可信事前版本，不能靠本轮测试倒推。

## 本机范围已做完，剩余交接理由

共享代码固定验收、依赖/路径检查、消费者祖先隔离、历史事件有界核对及脱敏整理均由惠普完成。未读真实模板/重扫真实文件/搜索或修改链接/真实采集/安装注册/恢复/发布快照。

真实M2四处、HP-006、HP-022/023需真实取证或受控副本授权；172未知项在荣耀拥有原文件，但未授权重复调查；RY-03须用户给旧版本证据或选择已核验归档。npm与记忆旧状态缺证据，不能由荣耀猜测代恢复。私有传输与回退目的地、真实操作范围尚须明确；不重复询问已选双向需求、首次荣耀为准和GitHub/离线大文件路线。

下一步只集中处理上述真实新增输入，或由执行端收敛包装器与兼容声明；不再让两端重复确认同一验收或重整共享材料。HP-03、HP-04及完整迁移不标完成。历史所有失败证据保留。
