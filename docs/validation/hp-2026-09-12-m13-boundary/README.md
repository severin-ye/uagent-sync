# 惠普 RY-M13-01 续行拒绝修复

输入交接 **8e4bb4b154c3671def2b82269850219ddbc73201**；独立 clone 固定该 HEAD，与 **ec827170ec24d78fd27e213d3cc3ff1d678a6377** 的 src/test/scripts/package.json/package-lock.json 无差异。
独立修复源码 **1ad915b9b9a45289dd9540408907ce593982e227**。本轮惠普验证通过，等待荣耀独立复核；HP-03整体仍进行中，迁移未完成。

## 失败与通过分别保留

荣耀原474/474通过和附加漏洞失败保留于 [荣耀原报告](../honor-2026-09-12-m13/README.md)，未改动。两个mjs及11-confirmed-gates.log已读取；其probe退出0是确认漏洞，不能当安全通过。

| 检查 | 退出码 | 结果 |
|---|---|---|
| 独立 npm ci（ignore-scripts）及基线构建 | 0 | 通过 |
| 基线原定向 | 0 | 56/56 |
| 修复前新增拒绝红测 | 1 | 34项，15通过、19预期断言失败；非环境故障 |
| 修复后四文件定向 | 0 | 90/90（原56 + 新34） |
| npm test，含pretest构建 | 0 | 508/508 |
| impact terminated upstream | 0 | HIGH，5上游符号，1直接调用者，3入口流程 |
| detect_changes staged | 0 | 2文件，5图符号，3流程，medium |

[命令及退出码](commands.json)、[红测](red.log)、[最终定向](focused.log)、[全量](full.log)、[实际影响](impact.log)、[提交范围](detect-all.json)、[隔离](isolation.json)。红态测试副本留惠普临时证据目录，没有提交红态代码；公开日志只包含人工样本诊断和脱敏路径。原日志摘要单列。

## 最小修复及边界核对

只修改 profile-expressions.ts 的 terminated 换行分支：拒绝逻辑与、位运算、余数、比较、相等、移位、in/instanceof 等续行；TS as/satisfies 保守阻断。原成员、调用、索引、模板标签、逻辑或、空值合并、条件及算术拒绝保留。逗号等不确定后缀也不据换行推定终止。未修改词法器、atom、掩码、基础扫描器或允许形式。

31个JS尾部样本各配LF、CRLF、单行注释和带换行块注释；均先用new Function仅解析、不执行，再验证原始第2行拒绝。覆盖运算符族和成员/调用/索引/模板标签。TS两类只作为保守拒绝，未声称由JS解析器验证TS。独立const/if/function语句与显式分号仍允许；既有++/--保守拒绝策略未扩大为允许。

三入口对同一JS样本配对：安全环境读取可采集、隔离恢复字节一致、publish到达mock门槛；跨行逻辑与必须拒绝，采集无产物，恢复目标及基线不变，publish无新增Git调用、无.git。红测会独立检查全部三入口，避免第一条失败遮住后两入口。恢复使用人工内容与重新计算的有效摘要，拒绝不是摘要错误。所有发布调用mock，无实际网络发布。

原56项完整保留：11处Python候选对应语法、注释凭据、邻接人工凭据、LF/CRLF/非BMP原始行号、M0基础扫描与Codex恢复调用者均复验。全量同时覆盖设备入口、中文搬运、Git元数据排除、路径/链接拒绝和冲突保护；临时安装测试不算设备插件安装。

## 影响与限制

实际链：terminated ← recognizeProfileExpressions ← assertProfileContentSafe ← createCodexProfile.walk / readSnapshot / publishDevicePaths.scan；已在修改生产符号前完成并向用户报告HIGH风险。图索引有流程预算截断、跨语言字段未连接和大JSON跳过提示，不能以缺少图边证明无影响；结合调用者源码与入口测试核对。detect_changes的5符号包含新测试局部符号，唯一生产符号为terminated。

这是有界词法识别器，不宣称成为完整JS/TS解析器。测试覆盖上述续行族，不证明所有语言语法安全。不扩展5处PHP/Ruby/TS占位符候选，不修改M2或链接功能。

## 未执行及交接

5处HP-001/004/005/007/008继续暂缓；M2、172项未知来源、RY-03旧链接版本绑定缺口继续保留。未重新搜索或替换链接。没有真实Skill采集、个人快照上传、安装、真实恢复、私有Git往返、双向同步或大文件迁移。真实配置和记忆未作为本轮输入，也未执行写入。

荣耀下一步：固定上述独立源码，隔离环境取消顶层UAGENT覆盖，构建、四文件定向及全量复验；复核31类JS续行与TS保守拒绝、三入口副作用保护、11/5范围和行号保护。对RY-M13-01给独立结论，保留旧失败证据；通过也不勾选HP-03整体或迁移完成。更新荣耀负责状态、推送脱敏报告并返回惠普提示词。
