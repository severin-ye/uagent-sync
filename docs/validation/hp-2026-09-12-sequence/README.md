# 惠普 HP-03-M13-SEQ：两种固定完整序列

**两种序列实现并通过惠普回归；既有13文件只读复扫为8通过、5阻断、7命中。** HP-001/004/005不再命中，其余七项完整保留。待荣耀独立复核，HP-03、HP-04及迁移未完成。

输入交接5075de32828a8d0c8e979f39303cd8a0fdb9b3ae，与af3f79c2c66a3d3c17fea20de0b12cc5577faf0f的src/test/scripts/package及lock无差异。独立短目录clone固定输入，完成红绿回归；独立修复源码 **70428520228adcac3ac66a2cfb857667b7b69aed**，随后在该提交上只读扫描13文件。文档另行提交。

已读取荣耀readonly-rescan的README、RESULT、comparison、candidate-mapping、region-review、SEQUENCE-REVIEW、commands及scan-script，接受既有5/8/10对照和有序完整区域门槛。

## 红绿与影响

- 修改前新增70测试：54通过，16预期失败，退出1。16为10个新允许/位置/多围栏测试、2个期望精确掩码的注释测试、4个三入口安全序列对照。不是16个新凭据漏检；54项旧版边界/语境保护为回归，未冒称红测。
- 新文件70/70，构建退出0；原七文件加序列文件的八文件定向286/286；完整npm test含构建704/704，均退出0，无跳过。
- lexExample实际impact HIGH、3上游；recognizePhpRubyExamples HIGH、5上游；各1直接调用者，影响采集/恢复/publish三流程，修改前已告知。
- 提交前detect_changes退出0；Git仅生产模块与新测试2文件，图映射2生产符号、9流程。新测试未由符号图映射，使用Git差异及实际回归补核。图仍有预算截断、大JSON跳过与跨语言字段缺边，不以缺边证明无影响。

命令、退出码和逐命令执行前环境见[commands.json](commands.json)。[红态](red.log)、[最终定向](focused.log)、[全量](full.log)、[变更检测](detect-all.json)。原始红态测试/日志及私有摘要留本机，公开材料脱敏；所有历史失败报告原样保留。

## 实现范围

仍完整消费整个专属PHP/Ruby词法区域，保留原单条构造语句分支。新增仅两个固定模式：PHP固定use导入、同变量空参数构造、同变量精确命名参数构造；Ruby固定anthropic字面量require、同变量无括号默认new、同变量精确关键字构造。不得重排、重复、换类/变量、动态导入或附加字段。PHP固定命名空间标识符作为单个原文token识别；Ruby要求导入和默认构造后有显式换行token，只合并连续空行/纯注释行终止符，保持原偏移。

整区域匹配成功后，只把显式构造中精确引号占位符的14个UTF-16单元替换为空格；导入、默认构造、注释和所有其他内容保留。未知token、尾部、续行、标签切换、插值、heredoc/nowdoc、百分号字符串/文档块等继续整区弃权，不逐行白名单、不执行导入、不解码字符串。大小写/别名注释拒绝及原带引号字段、跨行边界保护不变。

测试覆盖LF/CRLF、候选20/22行位置、非BMP前缀、多个围栏、空行/注释、精确整体掩码、重排/重复/异类异变量、导入别名/换包/动态/插值/拼接、额外语句、字段、非空默认、缺结束符、逻辑/比较/链式/块/反斜线续行、嵌入与未闭合模式、各语句前后敏感注释及邻接带引号值。

四组三入口（两语言×LF/CRLF）先以人工安全序列允许采集/恢复，并仅到mock发布门槛；危险注释再独立累积七项断言：无快照目录、恢复目标字节与baseline不变、无新增mock调用、无.git。坏快照重算有效摘要，不依赖摘要错误获得假拒绝。所有操作限人工隔离fixture，不涉及真实快照/恢复/网络发布。

## 真实文件只读结果

[scan-result.json](scan-result.json)保留全部23个旧ID的原始行号映射；13份旧摘要全匹配、0漂移、扫描前后13一致。8文件通过、5阻断，旧10命中降至7；三个PHP/Ruby完整文档本轮通过。Python11/TS2原候选依旧不命中，但openalex/openfda整体仍因M2阻断。

| 残留ID | 原始行号 | 规则 | 分类 |
|---|---|---|---|
| HP-006 | 263 | known-token-prefix | 来源/真实性未确认 |
| HP-009/010/013 | 100/125/344 | sensitive-assignment | M2参数说明 |
| HP-018 | 158 | sensitive-assignment | M2参数说明 |
| HP-022/023 | 216/340 | sensitive-assignment | 固定密码示例 |

总计6次sensitive-assignment、1次known-token-prefix，无新增未映射命中；不放行其他七项来凑全绿。纯扫描命令退出0仅表示检查完成，非所有文件安全。只声明13份两个读点摘要一致，不声明全机无并发变化。

PHP导入显示中的双反斜杠曾被误读；进一步核对原文代码点只有一个U+005C且旧摘要匹配，已在实施前更正，未据显示转义扩大规则或修改原文。namespace-check.json记录最终核对。

## 交接与剩余工作

荣耀下一轮独立固定源码，构建/八文件定向/全量，审阅序列边界及三入口；然后只读复扫共同13文件并核对摘要，验证8/5/7及HP-001/004/005消失、其余七项保留。只更新荣耀负责状态并返回惠普提示词。

M2、HP-006、HP-022/023、172未知来源与RY-03继续阻断；不重搜或替换链接。不做真实采集、插件安装、恢复或快照发布；未改真实配置、记忆、原文及原始报告。npm依赖仅安装在隔离clone。HP-03-M13-SEQ仅本机有限实现/验收完成，HP-03整体、HP-04和环境迁移不得标完成。

公开红态日志中4行人工断言值触发基础扫描规则，已替换为脱敏说明；失败名称、断言类型、数量、退出码及原始日志摘要保留。
