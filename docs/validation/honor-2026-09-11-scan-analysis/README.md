# 荣耀扫描与链接证据（RY-02）

本轮只整理证据，不修改扫描规则或真实文件。原阻断报告来自已验证源码 `d076fff50b142b4d09a2fbee9b0b8882b1554b96` 的荣耀复验；当前读取用于补上下文，不能证明历史时点的文件字节完全相同。

## 数量口径

- 82 个唯一文件，195 次规则命中，分布在 193 个唯一文件行；同一行可有不同规则命中。
- sensitive-assignment：169 次；known-token-prefix：19 次；authorization-bearer：7 次。
- 另有 1 处符号链接问题，不计入上述扫描文件数或命中数。
- 82 个文件均可读，195 条命中均有脱敏上下文，未出现无法定位行号；没有历史文件摘要，所以历史漂移仍无法判定。

## 证据文件

[逐处报告 report.json](report.json) 包含文件路径、规则、原行号、当前文件 SHA256、脱敏上下文、截断标记及不确定项。长行以推定命中位置为中心截取，最多360字符。主对话将195条路径/规则/行号与原报告逐项比较，并复核82个当前文件摘要，结果一致。

current_expression_shape 只描述当前行中可识别的环境/参数引用、文档或示例形式，不是凭据真实性判定。未确认项保持未确认。HP-01/HP-02 应据两机证据继续分类；不要把195次命中当作195个真实密钥，也不要直接放行所有示例文件。

## 链接检查

- 路径：`<USER_ROOT>/.agents/skills/research-skill.legacy-broken-20260826`。
- 类型：Windows SymbolicLink；`lstat/lexists` 能看到链接本身。
- 脱敏目标：`<USER_ROOT>/Codelib-severin/2_Business/Severin research skill/research-skill`，原始 readlink 结果带 Windows 扩展路径前缀。
- 当前有效性：目标不存在、无法解析；链接存在不等于目标有效。
- 可移植性：否；绝对路径包含源机用户根且目标失效。这里仅记录问题，具体方案由HP-02/RY-03处理。
- 仅只读元数据核查，没有删除、重定向、重建或修复原链接。命名中“broken”不是有效性证据，结论来自本轮检查。

## 脱敏与限制

用户根路径、疑似凭据赋值、令牌形字符串和URL查询值被遮蔽，保留路径相对位置与必要语法；不含完整源文件、个人快照或记忆正文。原始报告/文件保留荣耀本机。本目录校验和仅针对发布副本。

本轮主对话复核接管未完成的子代理整理，纠正了链接有效性字段；以最终report.json中的独立lstat/readlink/目标存在性检查为准。该过程不改变任何源文件。

RY-02证据整理完成不等于扫描阻断已消除；完整采集、安装与迁移均未完成。
