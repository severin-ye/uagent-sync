# 惠普 M0 独立修复验收

**HP-03-M0-R → F → V 已完成。M0定向14/14、隔离全量433/433通过；待荣耀独立复核，M1/M3未启动，完整迁移未完成。**

- 输入/独立源码基线：`884dea961d85b59d13f9526dfbc1211c0d6addbd`。
- 此前两机已验证源码：`d076fff50b142b4d09a2fbee9b0b8882b1554b96`。
- 本轮独立修复SHA：`11237d974ce42f3377cf06faf01e351b8f20cc3d`。
- 基线与d076fff的`src test scripts package.json package-lock.json`差异为空；荣耀本轮3项公开文件校验和通过，见[baseline-checks.json](baseline-checks.json)。
- 本目录的后续文档提交不改变上述修复SHA；荣耀应针对修复SHA复验，不能用d076fff旧结果替代。

## 实际改动及影响

仅将`src/lib/secret-scan.ts`中发现占位符就`continue`整行的语句替换为解释性注释。原三条规则、输入文本、换行拆分和每规则每行一条finding API均保持；不增加规范化或掩码，因此不存在M0替换文本改变UTF-16长度/换行的问题。

既有完整占位符本身不会匹配原规则，无需整行豁免。同一行另一处凭据现在继续检测。该修复不是通用语法分析器，也没有扩大占位符允许集合。普通变体文本不自动判凭据，但不能再掩盖会匹配规则的邻接凭据。

修改生产符号前，使用现有GitNexus 1.6.11在独立目录、隔离用户根建立索引并实际执行上游影响分析，均退出0。影响为**CRITICAL：22符号、4直接调用者、7受影响入口组、6模块**，修改前已向用户说明。直接调用者为`assertNoSecrets`、`assertProfileContentSafe`、`restoreCodexExtensions`、`writeSkillRecoveryReport`，详见[impact-summary.json](impact-summary.json)和[原工具输出](impact.log)。

图构建提示执行流探索有预算截断、跨语言字段未关联，并跳过1个大于512KB的荣耀证据JSON；不将缺失图边解释为没有调用。补充只读调用点核查及完整回归。暂存检测退出0，识别2个文件/11符号（仅1个生产符号，其余为新测试）、采集/恢复/publish三条相关执行流，工具风险medium；这是差异范围评级，不替代此前整体上游CRITICAL评级。见[detect.log](detect.log)。

## 红、绿及完整回归

| 阶段 | 命令（独立仓库根目录） | 退出码 | 实际结果 |
|---|---|---|---|
| 依赖 | `npm ci --ignore-scripts --no-audit --no-fund` | 0 | 仅独立源码开发依赖；没有注册或安装运行中插件 |
| 未修复构建 | `npm run build` | 0 | 固定基线可构建 |
| M0-R | `node --import tsx --test test/secret-scan-m0.test.ts` | 1 | 14测试、2通过、12预期断言失败；不是导入/环境故障 |
| M0-F | 同一定向命令、同一测试文件 | 0 | 14/14，失败0 |
| M0-V | `npm test`（含pretest构建） | 0 | 433/433，85 suites，失败/取消/跳过均0 |
| 提交前 | `git diff --cached --check`及`detect_changes --scope staged --repo hp-m0` | 0 | 仅M0源码与新增测试 |

原始红态补丁、测试副本及失败日志只保留惠普本地，不上传；本目录只记录红态统计、命令与日志/测试摘要。红绿测试文件摘要一致，正式修复提交前全部测试已通过。可复制测试文件到未修复基线重现，禁止将红态作为正式修复提交。命令退出码与原日志摘要见[commands.json](commands.json)，绿态及完整脱敏日志见[green.log](green.log)、[full.log](full.log)。

测试全部使用人工样本及临时目录，覆盖：

- 三类凭据在两类完整占位符之前/之后，同行重复匹配API，变体/前后缀/拼接旁的凭据、8千字符长行、脱敏结果。
- LF/CRLF、非BMP前缀、Unicode分隔字符和原始行号；直接扫描和profile扫描报告一致。
- 同一人工内容在采集、恢复读入、publish扫描均返回第2/3行规则；采集拒绝后没有快照目录，伪造但摘要一致的快照恢复拒绝后目标不变且无备份/基线写入，publish拒绝前外部命令调用为0且未创建Git目录。
- 占位符独立存在时基础扫描/采集/恢复预览兼容；M1环境空默认值、参数引用、M2参数说明、M3普通API占位符仍拒绝。
- Codex MCP恢复的人工凭据拒绝且执行调用为0；恢复报告残留凭据拒绝回归（该报告场景在基线已拒绝，是保护回归而非新增红测）。
- 全量覆盖原secret/profile/Codex恢复、导出/push调用链、Git元数据与旧快照拒绝、路径逃逸/junction、中文路径搬运、备份与冲突保护。全量中的打包安装测试只使用临时前缀，不是设备插件安装。

本轮测试进程及子进程的USERPROFILE/HOME/APPDATA/LOCALAPPDATA/CODEX_HOME/XDG_CONFIG_HOME均指向新临时根，OPENCODE_SYNC_WORKSPACE_ROOT指向独立源码克隆。GitNexus注册表也在隔离根；未改真实配置、记忆、原始证据、链接或插件缓存。构建产生的i18n换行变化不包含在提交中。没有声称对并发运行的真实环境做了全机摘要监测。

## 仍未完成与下一轮

M0是修补漏检，不是消除完整Skill采集阻断。此前13文件/23命中及荣耀82文件/195命中为历史证据，本轮未重扫真实文件，不推测修复后数量；移除跳过可能发现更多此前被掩盖的问题。

首批16个M1/M3候选仍未实施。M2四处、2密码示例、1未确认令牌及荣耀额外172项仍不获自动放行；25项RHS未解析、81fixture真实性未确认。沿用CURRENT-PLAN及TEST-DESIGN-V2，不因M0通过放宽后续门槛。

RY-03仍阻断：归档136项一致，但缺旧链接版本绑定证据；需旧版本证据或用户明确选择替代基线。不再搜索、不擅自替换，本轮无链接实验。

新版插件及Severin联动安装、全新完整快照、荣耀资料实际恢复、私有资料GitHub往返、双向同步、大文件迁移、惠普首轮记忆变化来源均未完成。报告推送只算证据交接。

交荣耀独立复核上述修复SHA及M0边界，在隔离目录构建、定向和全量回归，记录实际SHA/命令/退出码及脱敏日志；本轮先不推进M1/M3或真实采集。荣耀只更新自己负责的复核记录，不代改惠普已验收状态；发现问题则交惠普修订。完成后返回带实际交接SHA和报告路径的惠普提示词。
