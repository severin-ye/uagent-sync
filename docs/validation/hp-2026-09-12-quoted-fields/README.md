# 惠普 RY-C5-01：精确带引号字段拒绝修复

输入交接 **0b13fbb28b679ba16f89b74f39e091fae389a97e**。独立clone固定该HEAD，与 **04672a6314d66c745bb11819e90f71b5ced0d3d0** 的src/test/scripts/package及lock无差异。独立修复源码 **c96ee5bb130ad89e31f3d37724161c6ac80c2983**。

本机修复验证通过，等待荣耀复核；RY-10状态未代改，HP-03整体及迁移未完成。

## 结果与证据

| 检查 | 结果 / 退出码 |
|---|---|
| 独立依赖安装及基线构建 | 通过 / 0 |
| 修改前impact | HIGH，7上游符号、1直接调用者、3入口流程 / 0 |
| 首次移植测试 | 有fixture扩展名误改，不能把两入口用例的安全对照失败当有效漏洞复现 |
| 修正后的正式红测 | 16项15预期失败 / 1，含荣耀两组三入口复现 |
| 增补单引号三入口红测 | 18项17预期失败 / 1；四组三入口均7条拒绝/副作用断言失败 |
| 修复后六文件定向 | 139/139（原121 + 新18） / 0 |
| npm test，含pretest构建 | 557/557 / 0 |
| detect_changes staged | 2文件、5图符号、12流程 / 0 |

[命令](commands.json)、[红态](red-expanded.log)、[定向](focused.log)、[全量](full.log)、[影响](impact.log)、[提交范围](detect-all.json)。所有命令执行前保存隔离环境字段，见各pre-env.json；[实际wrapper脱敏副本](executed-wrapper.txt)含显式delete UAGENT动作，OPENCODE允许fixture覆盖。用户/Home/Codex/AppData/XDG/TMP/npm缓存与配置路径隔离。每次原始日志摘要单列；公开副本同时脱敏真实路径和JSON转义路径，原件留本机。

## 根因界定与最小修复

已读取荣耀README、REVIEW、RESULT、baseline-comparison、pure-probe以及三入口测试与失败日志。接受其结论：危险尾部单独在旧版也漏检；与占位符组合时旧版因占位符阻断，新版放行。**不是掩码删除了真实值。** [旧539通过与附加失败证据](../honor-2026-09-12-five-candidates/README.md)原样保留。

唯一生产修改位于recognizeProfileExpressions：既有TS冒号拒绝检查从裸apiKey扩展到精确单引号或双引号包围的同名字段token。quoted token不属于原构造语句allow-map，因此即使其值为占位符也继续拒绝。不改typescriptPlaceholders、lexer、M0/B1、Python逻辑或基础扫描器；不解码任意字符串为源码，不增加允许范围。

新测试覆盖单双引号字段的注释值、模板值、跨行非空值、行注释、插值及跨行块注释；LF/CRLF、原始字段行号、非BMP前缀、普通字符串不被解释/掩码、邻接凭据保留。纯函数同时测试尾部单独和与安全形式组合，断言危险尾部在规范化后仍完整保留。JS子集样本new Function仅解析、不执行。

四组三入口覆盖单双引号 × 注释/模板值：安全原形式先允许采集、恢复原字节及到达mock门槛；危险组合必须拒绝，无采集目录、恢复字节及baseline不变、无新增mock调用或.git。人工坏快照重算有效摘要，防止把摘要错误当扫描拒绝。未发生真实Git或网络发布。

这是一处精确字段token拒绝修复，不宣称覆盖任意编码字段名、计算属性或全部TS语法。原允许范围保持两处TS形式及11处Python形式的测试支持，不代表真实文件采集通过。

## 剩余范围及交接

PHP/Ruby三处未启动；其专用词法准入方案保留在荣耀REVIEW。M2、172项未知来源及RY-03继续阻断，无新来源证据不重搜、不替换。没有真实采集、安装、恢复、个人快照上传、链接操作、私有资料往返、双向同步或大文件迁移；真实配置、记忆及原始证据未改。

图索引有流程预算截断、跨语言字段及大JSON跳过警告；结合已有调用链和三入口运行核对，不以图中缺边证明安全。提交前detect仅涉及预期生产文件及新测试。

请荣耀固定独立源码，显式隔离并保存执行前环境，构建、六文件定向和完整npm test，独立复核RY-C5-01及四组三入口；保留旧失败并更新荣耀负责状态。复核通过后再明确PHP/Ruby下一轮任务，不在本轮启动。推送脱敏结果、核实远端SHA，并返回惠普可复制提示词。
