# 可另行启动的最小原型：HP-03-M2-G0

这是下一轮任务范围建议，本轮未执行。采用该任务的明确请求可授权隔离原型，不代表同意生产依赖接入、真实模板准备或M2放行。

1. 在独立临时目录建最小Node包，固定@lezer/python 1.1.18并记录实际lock、包完整性和许可证；不修改主仓package/lock、扫描器或插件缓存。不需要真实D正文。
2. 只用人工Python输入和人工模板。保存实际树名/父子/跨度，验证strict抛错与isError；特别测试候选前后未闭合短字符串及三引号，原始闭合核验不能只依赖strict。验证参数默认顺序、裸星号、默认Name错认形参、泛型非候选、逗号表达式、LF/CRLF/UTF16。
3. 独立人工provider模块，输入来自测试宿主注入；验证封闭schema、重复JSON键处理、版本、精确匹配、缺失、深冻结、一次加载及并发操作隔离。不得读取真实Skill或私有模板，不接采集/恢复/publish。
4. 显式隔离USERPROFILE/HOME/APPDATA/LOCALAPPDATA/CODEX_HOME/TMP/TEMP及npm配置缓存；删除顶层UAGENT_SYNC_WORKSPACE_ROOT，保存实际执行前环境（脱敏）。Node18与当前Node分别运行；缺运行时按环境阻断记录，不以当前Node代替。原型包打包后的import另测；真实uagent CLI/plugin发布兼容性仍留给后续集成。
5. 记录命令、退出码、树和通过/失败/未执行，兼容故障不是安全红测。若strict接受某种畸形输入，保存人工复现，验证原始闭合检查能否可靠使整文件弃权；不能因此扩大允许或换成整行例外。
6. 上述明确门槛完成即返回荣耀复核，失败提供最小例子，不自动开展生产集成。公开仅人工fixture、原型脚本及脱敏证据，真实模板准备另需明确操作者和本机私有路径。

不把G0通过写成M2通过。M2及其余既有阻断、HP-03/HP-04和环境迁移继续保持。
