# 荣耀 RY-14：M2具体范围复核

**有条件接受Lezer候选与私有精确模板方向；可另行启动人工数据原型，尚不允许生产接入或M2放行。** 已fetch并核对76b5456025f5f3e003cf8e3dd3387210a1d4894e。src/test/scripts/package及lock相对70428520228adcac3ac66a2cfb857667b7b69aed无差异。

本轮读取惠普m2-scope全部六份指定材料及共同清单；没有重读真实四处或172项，没有安装包、准备模板、改扫描器、运行新测试或真实扫描。

## 接受与修订

- 接受：固定纯Node候选、整文件解析和延迟提交候选、原始UTF16跨度、顶层同步函数受限形式、精确7字符掩码、缺模板不激活。
- 必须明确：strict不是Python完整词法/语义保证。固定上游tokens.js的短字符串分支可在换行处产生零宽stringEnd，须核验整文件真实引号闭合，不能只检查候选字符串或isError。
- 必须明确：param是小写grammar规则，不应假定存在逐参数命名节点；参数名/默认值/注解需按真实树和分隔符关联。还需拒绝不支持的TypeParamList、位置参数默认值顺序错误及裸星号末尾。
- 必须明确：可信provider注入层、封闭schema、版本来源、每操作一次冻结快照及并发隔离；三入口共享合同，不要求三次独立操作共享一个对象实例。

逐项见[REVIEW.md](REVIEW.md)。上游发现为静态证据及待测推断，不是已运行漏洞复现；未安装依赖，不能宣称strict在具体人工输入上的最终行为已经验证。

## 最小下一步

不需要真实D正文就能另行启动[人工原型范围](PROTOTYPE.md)：在独立临时包中验证固定依赖、实际节点、严格模式与原始闭合检查，以及人工模板provider合同。原型不能连接生产扫描器或三个真实入口。

原型启动所需最小输入是明确请求执行这一隔离实验；**真实模板的操作者、私有路径及审核正文不应阻塞纯人工原型**，只在后续真实启用前需要明确。不能把本范围复核或原型通过当生产集成授权。

现阶段Node18、当前Node运行、npm实际包/lock依赖、发布后CLI/plugin兼容性和资源上限均未实测。只读查阅上游API与这些运行门槛分别记录于[SOURCES.json](SOURCES.json)和[RESULT.json](RESULT.json)。

M2四项、HP-006、HP-022/023、172未知项及RY-03继续阻断。未搜索/替换链接，未采集/安装插件/恢复/发布快照，未修改配置、记忆、原文与历史证据。仅更新荣耀RY-14；HP-03、HP-04与环境迁移仍未完成。
