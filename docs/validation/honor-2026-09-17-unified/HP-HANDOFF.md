# 惠普下一批执行范围

沿用整体授权及 DEVELOPMENT-WORKFLOW，一次完成本机可做事项后集中回传。

1. 核对本批源码提交及私有包摘要。已完成的 7415 接收、原 869 项和 marker 往返不重跑。
2. 升级前保存现有插件清单、配置、2.1.2 与并行设备版来源。先核对远端 master 确实包含统一提交，再通过 `codex plugin marketplace upgrade uagent-sync` 与 `codex plugin add uagent-sync@uagent-sync --json` 更新正式插件；不把版本较低的旧设备分支覆盖回去。
3. 新缓存没有 dist 时，只为已核对的新版本安装锁文件中的生产依赖；使用显式 npm --prefix、隔离 npm 配置/cache、--omit=dev --ignore-scripts --engine-strict。从同摘要安装包补齐已验证 dist，并逐文件核对。不得借此替换办公配置。
4. 用新宿主 skills/list 确认统一包五个 Skill 被发现且启用；检查各旧 marketplace 的重复入口，只在新入口可用且已备份后停用重复 Skill，旧包可保留回退。保护其他插件设置。
5. 在新目录运行安装版设备配置副本预览/恢复、路径转换、身份保留、双改冲突，以及安装版结晶的安全中断续传。真实办公文件不作为测试目标。报告 Hook 信任和账户登录的实际状态，不绕过审核。
6. 133 份离线资料清单位于私有 `handoffs/honor-hp-20260917-offline/`。有真实移动盘/共享目录才搬运；先预览后 Apply，再目标逐文件摘要核验，缺通路就保持未传输。207 研究材料继续等待 TabPred 完整父子集成，24 停用项不启用。

只处理新增版本和本机剩余事项。程序、安装、真实业务、完整迁移分开报告；没有新的必要疑点不重复全量测试。返回真实提交 SHA、回退位置、安装与验收结果及一次集中提示词。
