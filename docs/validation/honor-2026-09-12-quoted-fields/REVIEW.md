# RY-10带引号字段修复审查

固定c96ee5bb130ad89e31f3d37724161c6ac80c2983，相对04672a6314d66c745bb11819e90f71b5ced0d3d0。输入445142e0dc99bcc583bdd72b2d201ccd854ebecb。惠普27份公开文件摘要核对一致；指定报告、RESULT、命令、wrapper、full-pre-env、红态、定向、全量、impact和detect均已读取。

## 生产差异与允许范围

唯一生产改动是recognizeProfileExpressions内冒号字段检查：裸apiKey扩展为精确apiKey、"apiKey"、'apiKey'三种token。typescriptPlaceholders与其Map未改；引号字段没有进入允许Map，即使值为占位符仍拒绝。没有把字符串解码为源码，没有改词法器、原行号计算、掩码、基础扫描器、Python或M0/B1边界。本次是拒绝范围补齐，允许范围没有扩大。

原121项测试没有删改，新18项检查单双引号各6种值语境、两个范围/行号测试及四组三入口。注释、模板、跨行非空值、行注释、插值和跨行块注释在LF/CRLF下均要求原字段行号；危险尾部经过规范化仍完整保留。非BMP前缀未改变原始行号；普通转义字符串保持不解释、不掩码。

四组三入口为单双引号乘注释/模板：先验证原安全形式可以采集、恢复原字节并到达mock Git门槛，再分别断言危险文件拒绝、无快照目录、恢复目标字节和baseline不变、mock调用不增加、无.git。坏快照重算正确摘要，避免误把完整性拒绝作为扫描通过。每入口独立收集断言，前项失败不遮蔽后项。

惠普影响分析HIGH，7上游、1直接调用者、3流程；detect两文件5图符号12流程。这里只核对惠普证据，不宣称本机重新建图。未发现本次精确字段修复新增实质缺口；结论限本次识别范围，不宣称任意编码字段、计算属性或全部TS语法完备。

## 历史证据边界

保留上轮539全绿及附加两组三入口失败。危险尾部单独在旧版也漏检，组合占位符后整体从阻断到放行，不是掩码删除真实值。本轮使用同一旧人工脚本在新固定源码重跑，结果单列，不修改旧日志。运行结论及退出码见README/RESULT。

## 下一轮PHP/Ruby最小任务（本轮不实施）

若运行复核通过，惠普可继续HP-001、HP-004/005三处的专属有界词法识别：仅PHP Client的apiKey命名参数和Ruby Anthropic::Client.new的api_key关键字参数，精确引号占位符须为完整值；先固定各自完整语句/参数边界，不泛化其他调用、字段或字符串。

红测门槛：语言专属注释、heredoc/nowdoc、Ruby百分号字符串和文档块、插值、转义与续行必须与真实代码区分；嵌入示例不得获得掩码；非空拼接/回退/相邻字符串/附加字段及未闭合结构必须拒绝。LF/CRLF、原跨度及行号、邻接凭据、三入口允许/拒绝与无副作用配对齐全。现有基线已经拒绝的用例是拒绝回归，不冒称红测失败；需要支持的安全候选在修改前应有真实失败证据。

只做可证明的小语法，不能把PHP别名映射JS或Ruby映射Python直接放行，不要求安装语言运行时或执行被检代码。不能识别的保持阻断并列最小缺口；代码修改与报告分开提交，完成必要定向及全量回归后交荣耀复核。

Python11与TS2形式不等于真实文件已采集。M2、172项未知来源、RY-03保留；不重搜或替换链接。本轮没有真实采集、安装、恢复、个人快照上传或私有资料往返；HP-03及迁移不结项。
