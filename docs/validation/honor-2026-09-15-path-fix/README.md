# Windows配置路径转换修复

本批接收a4b5eb48dc5af8f68011bcc516f6f0654fc7eb46；其生产基线为1056d411419082bd18ad8f76fbb23720a3a3b93d。实际回程在固定旧版完成，随后修复导出端正反斜杠不一致导致来源目录残留的问题；没有改写旧profile或真实配置。

## 改动与边界

只改codex-profile.ts的导出路径替换：同时识别正斜杠、反斜杠和Windows扩展本地路径前缀，长根优先；检查两端边界，避免相似用户名目录及未知紧邻前缀被改写。扫描位置与输出位置分离，拒绝替换的片段仍完整保留。导入token替换、凭据剔除、扫描和冲突保护没有改动。

静态调用链为portableConfig→transform→mapString，影响新配置快照导出；同函数导入分支保持不变。GitNexus query提示索引缺失，impact未找到mapString、风险UNKNOWN；detect_changes只看到文件、没有符号，不能将其low输出当作完整影响证明。通过代码审阅、相关测试和实际配置受控副本补充验证。

新增人工用例覆盖两个设备根斜杠方向、命令参数、扩展路径、相似根、未知前缀、原文保留、快照源不变及恢复结果。最后另补同一字符串先有相似根、后有有效根的断言，final-extra.log单独通过；该断言不改变生产代码。

## 证据说明

red.log是在固定1056d41 dist上回放人工用例，退出1且为路径token/相似根断言失败。red-environment-failure.log是主端第一次回放把Windows绝对路径写成模块URL造成的环境错误，不算产品红测。baseline-replay.manual.ts保存修正后实际脚本；baseline-replay.txt保留最初错误URL。执行代理遇到账户用量限制后，主端接手运行并保存本目录结果，没有把代理未完成工作称为通过。

build.log、focused.log、full.log、commands.json及isolation.json记录主端实际执行。全量结果以full.log末尾和RESULT.json为准。finish-path.ps1保留第一轮实际wrapper，后续修正红态URL与附加断言单列，不把wrapper初始退出1直接当产品失败。

另使用既有惠普配置受控副本生成新派生快照，恢复后原四处来源路径残留为0、转换后SHA匹配且原profile未变。该副本不写真实用户目录，不证明目标机器宿主版本目录存在或插件可加载；原文和完整证据仅在私有交付或本地。

## 接入与使用

修复源码需在惠普独立目录构建，并对新增路径用例及实际配置副本核验。已有旧快照不会被自动修好；后续从当前真实配置按正式操作重新导出新快照，先预览再应用，保护本地配置与设备身份。宿主生成的运行时、插件市场版本目录必须按目标安装位置保留或重建，不能仅替换用户名。此源码交付不等于已安装插件升级，也不意味着完整环境迁移完成。
