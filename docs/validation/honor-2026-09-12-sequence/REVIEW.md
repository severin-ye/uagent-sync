# RY-12：受限序列独立审阅

固定70428520228adcac3ac66a2cfb857667b7b69aed，比较af3f79c2c66a3d3c17fea20de0b12cc5577faf0f。生产差异仅profile-php-ruby.ts，另新增序列测试；没有修改原单语句测试或其他扫描规则。交接文档与固定源码一致，34份惠普材料摘要核对一致。

## 完整区域与语句边界

PHP lexExample新增一个精确的带命名空间标识符token；尾随标识符字符或另一个分隔符即弃权，不做反斜杠解码或通用导入解释。荣耀只读核对HP-001旧摘要匹配，原第16行分隔符代码点为单个92；显示中的转义不会用来改原文。

recognizePhpRubyExamples保留原单语句pattern，新增固定prefix再接同一显式构造pattern。PHP固定导入的分号、默认空参数构造的分号与显式构造分号必须完整存在；Ruby固定包名字面量require和默认无括号new后必须有换行token。只折叠连续换行token来容纳空行/纯注释行，不改变token原始start/end，不逐行白名单。

匹配要求tokens总长度与全部token相等；顺序变更、重复语句、不同变量/类、动态导入/别名、额外字段和未知尾部无法匹配。失败整区不掩码，并保持原敏感字段拒绝；未知词法模式、跨行拼接/逻辑/比较/链式/块等不被切成安全子句。只承诺这个有界检查范围，不宣称通用PHP/Ruby解析完备。

## 掩码、原行号与注释

仅显式构造值token的精确单双引号占位符被等长替换（14个UTF-16单元）。导入、默认构造、注释和其他文本原样保留。LF/CRLF、非BMP前缀、原20/22行位置、多围栏与跨行参数均有覆盖。敏感注释的大小写/别名规则不变，命中偏移取原comment位置，原RY-PR-01、带引号字段和续行测试未删除。

新增70测试中54项旧版已拒绝，16项红态包含新安全允许、精确掩码与安全序列三入口对照，不是16个凭据漏检。未知语境测试检查不获掩码，携带敏感字段的边界测试另检查拒绝。

## 四组三入口

PHP/Ruby乘LF/CRLF共四组。安全人工序列先走采集/隔离恢复，并仅进入mock Git门槛；危险注释以原第4行拒绝。七项断言独立累积，分别验证拒绝、无快照目录、恢复内容与baseline不变、无新增mock调用；人工坏快照重算有效摘要，避免由摘要校验失败冒充扫描拒绝。没有真实网络发布或真实恢复。

本轮测试与只读复扫结果由RESULT及日志给出；未在上述差异和测试范围发现新增实质缺口。若原有测试通过，仅签收两种受限序列，不声称所有敏感内容均可识别。

## 下一步范围

PHP/Ruby三处从完整文件扫描消失后，剩余七项分别为M2四处、HP-006来源未确认一处、HP-022/023密码示例两处。172项未知来源和RY-03不在本轮范围。密码示例不能按常见值全局豁免，未知来源不能改判安全。

建议下一轮惠普执行HP-03-M2-PRE：仅针对旧ID HP-009/010/013/018，核对旧摘要后只读识别完整参数说明语法、字符串与可执行表达式边界，输出脱敏结构和最小专属规则方案及拒绝测试清单；不实现或放行，不输出真实值。尤其必须覆盖说明字符串与同行真实赋值、非空回退/拼接/插值、未闭合字符串及原始行号，不能“包含help/注释就整行跳过”。其余来源与密码、链接不混入本方案。随后由荣耀复核这一独立设计，明确实施边界。

| 事项 | 现在是什么样 | 准备改成什么 | 依据和理由 |
|---|---|---|---|
| M2四项 | 参数说明仍被阻断 | 下一轮仅补完整语法证据及有界方案 | 概念上是说明文本，不足以证明整段没有凭据 |
| 其他三项/172/链接 | 来源或处理依据仍不足 | 保留阻断，不与M2混合放行 | 防止以示例身份或测试全绿替代来源证据 |

本轮没有真实采集、插件安装、恢复、快照发布、链接操作或原文修改；扫描前后摘要只代表13文件两个读点，不证明全机无并发变化。HP-03、HP-04及环境迁移未完成。


## 本轮隔离失败与恢复边界

首轮八文件286/286，但全量681/704，23项失败集中在工作区定位。首轮PowerShell wrapper用SetEnvironmentVariable(...,$null)后，执行前证据中UAGENT_SYNC_WORKSPACE_ROOT实际为字符串空值，未真正删除。src/lib/cache.ts:176使用空值合并选择变量，空字符串仍优先于测试fixture的OPENCODE根，解释了定位失败。

这属于本轮wrapper缺陷，不能把该首轮环境记录称为满足显式取消要求，也不把23项冒称新增源码回归。恢复使用Python构造子进程环境并pop变量，保持固定源码及其他隔离目录，补子进程变量存在性证据后重跑；两轮日志分别保留，最终结果以RESULT记录实测为准。首轮还遗漏OPENCODE_CONFIG_DIR的显式清除，后续核对宿主未设置；恢复和扫描显式删除，不补造首轮执行前证据。


子代理另外准备了沿用空值删除方式的恢复脚本，并在TEMP祖先目录放置人工.gitmodules标记；主对话未将它作为有效恢复，停止子代理后将标记改名保留，避免以祖先发现掩盖变量问题。没有该准备脚本的有效恢复结果可用于验收。正式恢复由主对话Python子进程环境执行，保存Object.hasOwn检查全部false；未改变生产代码或真实文件。该额外准备与首轮失败均如实记录，不冒称单次正确执行。
