# 荣耀 RY-07 复核与 RY-03 链接方案

交接输入：`866e91967b56a5fec89a7789c43b350fd230120c`。两机此前验证源码仍为 `d076fff50b142b4d09a2fbee9b0b8882b1554b96`。本轮只读分析及形成方案，没有执行HP-03、添加生产测试、修改扫描器或重跑npm test。

## 结论与证据

- [M0–M4复核及测试补充](REVIEW.md)：M0接受先行独立修复；M1接受有限候选但须补跨行/完整表达式边界；M2建议首批暂缓；M3精确占位符候选接受；M4继续阻断政策接受。首批允许候选建议16处，不承诺快照通过。
- [172项逐ID语法证据](additional-syntax.json)：69文件，172个merged-findings ID，178个原始匹配跨度，80个遮蔽后结构组；每个ID均有映射。所有当前文件摘要匹配上一轮荣耀报告，分析前后摘要亦一致。没有历史更早字节可用于额外追溯。
- [链接处理候选与隔离范围](LINK-PLAN.md)：仅方案，未修改真实链接，未执行隔离替换实验，RY-03整体未完成。
- [本轮分析校验](analysis-checks.json)：统计、ID映射、列坐标与原规则逐处匹配、文件摘要和发布检查的结果；不是新源码验收。

## 证据字段与限制

原始匹配列使用UTF-16码元、1起点、右端不含；绝对偏移0起点。AST位置使用TypeScript的0起点行/字符；报告行定义与基础扫描器一致，仅按LF/CRLF分行，不把字符串内Unicode分隔字符另算一行。采集的是规范化前原始文本上该规则在报告行的全部匹配，不能将178当成172条不同历史报告，也不能推断其他行没有凭据。

RHS值被遮蔽，但保留节点类型、成员/调用结构、运算符、字面量存在性、非空字符串、fallback/条件/加号/模板标志。加号不是已证明的字符串拼接；引用/生成函数名字不能证明运行时绑定安全。组只是相同遮蔽结构的索引，不是安全允许名单。

25项未能建立赋值RHS：19项匹配落在字符串/模板内，另6项是未解析或非赋值语法。它们均有精确规则匹配范围与来源摘要；字面量/回退/拼接语义未确认时明确留空或未解析，不把缺字段当作false。全部172项仍`automatic_allow=false`。

81个fixture候选逐项记录当前版本/文件摘要、可见调用结构或缺口；没有因测试目录名认定其合成性，`synthetic_value_confirmed=false`。需要进一步值来源追踪的保留未确认。

两个长行：RY-104在1185行有4处匹配列[6461,6481)、[6701,6721)、[6947,6967)、[7199,7219)；RY-137在4550行有1处[17219,17268)。完整跨度均有指纹与结构记录，不再依靠截断窗口推断整行。

安装来源记录指向garrytan/gstack，当前包版本1.68.3；本地没有Git checkout，因此未取得上游commit。diagram-render的BUILD_INFO与当前bundle、入口加构建脚本摘要一致，固定依赖版本见JSON；构建未配置source-map，没有可验证的逐跨度上游模块映射。这些限制不影响“已补证”的计数，但影响后续安全分类和放行决定。

用户根路径和所有字面量值均不进入结构表达式，原始文件/报告保留本机。公开材料未包含个人快照或凭据；仍不能把模式脱敏检查当成任何未来文本均安全的保证。

下一端惠普处理荣耀意见、更新HP-02的最终候选范围和测试门槛，再明确后续HP-03分段安排；本报告不冒称方案已被惠普接受、测试已通过或迁移已完成。
