# RY-03：链接处理候选与隔离验证范围

状态：仅方案，未执行隔离替换验证；RY-03整体未完成。原链接、目标及真实Skill目录均未修改。既有证据见上一轮RY-02，不能只凭名称含legacy/broken删除它。

已知原链接：`<USER_ROOT>/.agents/skills/research-skill.legacy-broken-20260826`；绝对Windows目标为旧的`<USER_ROOT>/Codelib-severin/2_Business/Severin research skill/research-skill`。上一轮元数据检查表明链接本身存在、目标不存在。

## 推荐：先核验原Skill来源，再决定表示方式

方案A：若能从原仓库/已验证发布记录确定期望Skill内容，在新的临时用户目录中复制完整实体Skill副本。对比文件清单、每文件摘要、规则原文和相对引用，验证同一内容在不同用户根目录下可采集/恢复。当前Severin插件的同名或相似科研Skill不是旧链接等价性的证据，不能直接替代。

方案B：若该组件确实必须保留链接，在隔离目录设计显式来源映射：稳定组件ID、来源版本与摘要、设备本地目标路径。转换只由适配器执行；目标缺失、越界、嵌套链接/循环、版本不符必须报告并阻断，不按设备别名硬编码。此方案涉及新功能，不进入本轮或首批HP-03扫描修复。

在来源未确认前，继续保留真实链接与采集阻断。不要静默排除该Skill，也不要把旧链接失效当成用户不再需要该Skill的决定。

## 后续隔离验证范围（本轮未运行）

1. 新建临时目录，仅放入人工构造的Skill和链接；不得把真实目标作为可写测试位置。
2. 覆盖有效相对/绝对链接、失效链接、外部目标、循环、目标在采集中改变、来源摘要不符。
3. 若验证实体化副本，保证源内容逐字节保持、manifest覆盖完整、路径转换不改原文；失败不改写来源或目标。
4. 若验证映射方案，分别证明身份不串机、缺失时阻断、可审阅备份与回退，且拒绝越界写入。
5. 输出文件列表、摘要、允许/拒绝结果及原真实链接检查结果；只有隔离验证与来源核查完成，才能提出真实路径变更的明确范围、备份和回退方案。

本轮不需要用户选择真实替换方案，因为来源等价性尚未证明；交惠普在HP-02中记录依赖，荣耀后续RY-03继续核查。
