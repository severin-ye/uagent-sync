# 荣耀RY-17复核：不通过，RY-M2-01待修

已核对交接`cdefb187a2b00c0c92304cdea4330a8c40c518d6`，独立短目录固定源码`416120afbf2446463864e8292d1423e493055944`，比较`70428520228adcac3ac66a2cfb857667b7b69aed`。仅更新荣耀证据和清单，未修生产代码。

| 验证 | 荣耀实际结果 |
|---|---|
| 当前Node24.16.0构建、八文件定向 | 退出0；115/115 |
| 当前Node完整npm test | 接手重跑819/819，退出0；首轮执行异常保留 |
| Node18.20.8八文件 | 115/115，退出0 |
| Node18完整npm test | 705项：682通过、19失败、4取消，退出1；失败/取消标签与惠普旧基线相同，本轮未重跑旧基线 |
| 两类消费者×两运行时 | 纯内容/工厂/CLI及声明检查通过；omit-dev插件主入口仍缺宿主依赖；显式plugin/SDK1.18.15后导入通过 |
| 主对话附加纯内容检查 | 两Node各24次检查，16通过、8失败；合法换页空白绕过残留赋值保护 |
| 主对话附加三入口配对 | 两Node各6组，4通过、2失败；危险输入生成了人工快照、目标/基线，并触达被拦截的首次外部调用 |

**既有回归通过不能抵消新增反例。** RY-M2-01见[完整复核意见](REVIEW.md)及[人工证据](artificial/)。U+000C置于键与赋值符之间或值前、注释第二赋值及参数默认值均有LF/CRLF反例；8组Python AST合法。基础扫描对括号值的旧盲点与M2组合后由阻断变放行的影响分开记录。

[运行证据](runtime/)、[执行异常与隔离边界](EXECUTION.md)、[机器结论](RESULT.json)。公开只有人工脚本、脱敏日志与统计；不含快照/目标或真实模板。消费者先有private manifest、显式prefix且祖先无宿主SDK，未照搬惠普误定位命令。源码工作树显示生成的i18n文件修改，但取证确认其字节SHA与HEAD一致；核心扫描/入口/依赖diff为空，未强制回滚隐藏状态。

下一轮惠普先针对RY-M2-01保留红态并最小修复拒绝边界，保持现有允许范围，再回传独立SHA。M2真实四处、HP-006、HP-022/023、172未知项、RY-03、Node18兼容、omit-dev宿主依赖和惠普npm环境事件继续单列。惠普原状态未知，未由荣耀代处理；本轮未读真实模板、重扫、操作链接、真实采集、插件注册、恢复或发布快照。HP-03、HP-04与迁移未完成。
