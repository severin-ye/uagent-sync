# RY-15：人工原型边界审阅

复核对象为交接c25f06c441cf0dbd3345e0475ed6c3ecd4540760归档的prototype附件，不是新生产修复。生产目录及依赖相对70428520228adcac3ac66a2cfb857667b7b69aed无差异。

## 已有合同与保守差异

1. 全文件strict解析、全部可见String原始闭合和FormatString弃权先于pending产生；后置未闭合短字符串不会仅靠strict判安全。后置参数错误通过empty返回撤销整个pending数组。这两条路径须由本轮独立运行记录验证，不能由静态顺序单独宣称通过。
2. parameters增加原文间隙覆盖，解决命名节点遗漏匿名分隔符的问题；参数名取每组首节点，默认Name不加入名字集合。仍是受限结构检查，不等于CPython完整语义。
3. 接受原型额外检查所有顶层普通函数参数的保守差异：非目标函数的非法顺序也令整文件M2弃权。该差异只减少允许，报告必须注明；不可因此承诺真实四处所在完整文件都会通过。async/泛型跳过、FormatString全文件弃权均维持有限范围。
4. provider的WeakSet只证明由本模块创建了模板，不隔离任意执行代码；真实可信宿主注入点尚不存在。对象深冻结、每begin加载一次、操作内复用和并发对象分离是人工合同结果，不能替代实际采集/恢复/publish接线。
5. 版本目前是常量加安装inventory核对，不是生产供应链身份验证；通用unavailable原因将多个失败合并，比早先细分原因设计更粗。可接受为原型保守输出，生产日志合同需明确而且不能带正文。
6. 资源上限是输入长度/模板字节拒绝门槛，不是CPU、内存和极限解析性能已验收。纯字符串API不证明输入原字节未损失；测试里的严格TextDecoder并未成为生产读取路径。
7. 邻接人工赋值在跨度外只能证明原文保留；基础扫描是否拒绝、三入口是否无副作用均未执行。本轮不接真实入口、不准备模板、不重读真实Skill。

## 历史证据

惠普prototype-initial.log保留普通对象模板冒充及非JSON空白导致的2项失败；signature-red-node18.log保留api_key加位置分隔符导致的1项失败。它们是旧原型人工失败，不记成荣耀本轮新红态，不修改或删除原材料。strict风险probe由本轮直接重放，必须核对断言信息、接受的shortBefore/shortAfter及零错误节点，不能以加载失败代替。

## 待定范围

Args归属不仅是“存在更早Args标题且缩进更深”。必须确认目标是该节的直接参数条目，未知段落/嵌套示例不能借用函数形参名称获得跨度。

## RY-G0-01：Args未知语境误获跨度（复核阻断）

主对话在未修改的归档原型上运行additional-review.test.mjs。两运行时各7项：4通过、3断言失败，退出1。每个拒绝用例同时记录LF/CRLF结果。

- 正常直接条目通过；后置语法错误、未闭合短字符串、非法参数表三组均返回空跨度。
- Args下先出现更深的Example标题、再出现更深api_key条目，误返回1跨度。
- Args下先出现同级未知文字段落、再出现api_key条目，误返回1跨度。
- 正常函数之后追加上述嵌套示例函数，返回2跨度（早先正常1个及错误候选1个），没有按未知Args语境合同撤销整个pending。

根因位于附件parser.mjs第56–57行附近：只要求目标缩进深于标题，并排除中间缩进不深于标题的文字；未验证中间内容是合法直接参数条目，也未排除更深的未知示例段。整文件语法解析无法判断文档字符串内部的Args语义归属。

这证明原型违反未知段落弃权/Args直接归属合同，**不是已证明生产凭据泄漏**：原型未集成、仅返回字段跨度，不运行基础扫描。原57项及打包import通过仍保留，但RY-15不能判整体通过。

下一轮先在独立人工原型副本修复此问题：定义Args直接条目缩进/相邻条目/说明续行的有限状态；未知内容或嵌套语境无法证明时整文件M2弃权，不能删除原文或通过只比较描述来放行。保留本附加3失败作为先行红态，补直接多参数、合法Returns/Raises和空行的允许配对，重跑原57项及附加7项、双运行时和打包消费者；之后再交荣耀。不得启动生产集成、真实模板或真实扫描。
