# 荣耀 RY-M13-01 边界修复审查

固定对象1ad915b9b9a45289dd9540408907ce593982e227，交接240c0b454ff173389fe2450c7d25d34680332ab6。

## 生产差异

相对ec827170，生产只改profile-expressions.ts的terminated换行分支，补齐逻辑与、位运算、余数、比较、相等、移位等运算符前缀，以及in/instanceof/as/satisfies。词法器、atom、掩码、基础扫描规则与各入口没有改变；新增profile-secret-scan-boundary.test.ts，原56项测试没有删除。没有扩大允许形式。

惠普17项公开文件校验和通过；README、RESULT、commands、red/focused/full、impact、detect-all、isolation均已读取。impact是带CLI尾部的日志，主对话初次将整文件当JSON失败，改为解析JSON前缀后核对HIGH、1直接调用者、3入口流程；这是取证格式问题，不是程序失败。该影响分析属于惠普执行证据，本轮不冒称荣耀重新建图或修改生产符号。

## 合法续行与行号

新增31类JS尾部各配LF、CRLF、单行注释和跨行块注释，并用new Function只解析不执行。断言原始第2行敏感赋值被拒绝。类型断言as/satisfies仅作为TS保守拒绝，不冒称JS解析器证明TS语法。显式分号及独立const/if/function语句保留允许；++/--仍保守拒绝，未扩大允许集合。

原56项覆盖Python11处形式、元组/闭括号/邻接字面量/注释凭据、原始行号与M0调用者。此次没有改token区间或掩码方法，因此不新增文本位置转换。源码审查未发现本次B1新增实质缺口；结论仅限已验证边界，不证明全部JS/TS语法的扫描完备性。

## 三入口配对

同一人工安全JS文件先成功采集、恢复原字节，publish到mock门槛。跨行逻辑与的拒绝检查使用独立check累积失败，再统一断言空列表；采集失败不遮蔽恢复和发布检查。验证内容包括：

- 采集报告原始行号，拒绝后没有快照目录。
- 人工恶意快照重算有效摘要后恢复仍拒绝，目标字节及profile-baseline保持。
- publish不增加外部命令计数，无.git目录；全程mock，不真实联网。

这直接覆盖上轮RY-M13-01观察到的三个入口漏检。实际本机测试计数和退出码见README/commands，不以静态审查代替运行。

## 范围和下一步

11处Python语法形式不等于11处真实文件已采集。HP-001(PHP)、HP-004/005(Ruby)、HP-007/008(TypeScript)五处继续暂缓；M2、172项未知来源、RY-03等不变。

若本轮运行通过，下一轮惠普可以围绕这五处精确占位符逐语言补允许/拒绝配对，先红测再最小实现再全量回归，固定范围并独立提交。不能用通用文本或整行白名单放行；无法可靠识别的语言保持阻断并说明具体缺口。不要重新讨论已一致的M0/M13架构。

本轮没有真实采集、设备插件安装、真实恢复、私有资料往返或链接操作；HP-03整体及迁移不结项。旧失败证据继续保留。
