# 拟议红测、拒绝测试和集成回归

状态：**全部为设计，未写入test目录、未运行，也未执行HP-03。** “应拒绝”表示使用人工构造的非占位符凭据样本检查扫描行为；不使用真实用户密钥，不向外部服务测试有效性。

| ID | 场景 | 预期 | 对应方案 |
|---|---|---|---|
| T00 | 同行同时包含合法占位符标记与另一处人工构造的长令牌；分别测试hidden标记和YOUR标记 | 必须仍拒绝另一处令牌；新增红测暴露目前整行跳过问题 | M0 |
| T01 | 一个安全表达式前后分别放非占位符敏感赋值、Bearer和已知令牌前缀；同一行两个规则及同规则多处 | 每个真实值形态仍使采集拒绝；保留正确行号，不因安全跨度而吞掉同行其他命中 | M0 |
| T02 | 已有单参数环境读取、Python空默认值环境读取；单/双引号及合法空白 | 允许无凭据值的完整表达式 | M1；新增空默认值场景应先红 |
| T03 | 环境读取第二参数为非空字符串、或使用非空or回退、拼接、模板值 | 保持拒绝；不能只识别表达式前缀 | M1 |
| T04 | 观察到的args.api_key完整赋值、调用实参、跨行合法实参 | 仅完整已识别形式允许，行号映射不变 | M1；应先红 |
| T05 | args.api_key旁再给另一个参数嵌入人工构造凭据；args.api_key加后缀/索引/调用/非空回退 | 不属于已支持完整引用的部分不豁免；嵌入值仍拒绝 | M1 |
| T06 | 单纯未加引号的未知标识符、常量名、函数调用和属性对象；任意字段名含key/token | 不做全局变量名放行；未知按既有策略处理 | M1范围控制 |
| T07 | Pythondocstring中精确参数说明四种观察形式 | 在限定上下文允许说明跨度 | M2；应先红 |
| T08 | 同一docstring中增加真实值形态；普通配置文本假装Optional说明；说明后同一行追加令牌 | 真实值仍拒绝，不能忽略整个docstring/注释/说明行 | M2 |
| T09 | 七处API占位符的合成最小语法例子：Python、PHP、Ruby、TS | 仅完整已认可的精确占位符允许，不以文件名识别 | M3；应先红 |
| T10 | 占位符附加任意前后缀、相邻字面量拼接、同一参数替换为非占位符长值 | 保持拒绝 | M3 |
| T11 | qpdf固定密码示例、未确认token字面量、普通配置中的弱密码字符串 | 首批仍拒绝，不加固定密码/文档目录白名单 | M4 |
| T12 | 带test、fixture、dist、.env.example路径的人工构造凭据；在已审阅文件末尾新增凭据 | 一律仍拒绝该值形态，不能用目录或文件审核免扫整文件 | M4 |
| T13 | TS的环境引用旁有非空空值合并回退；生成token函数旁有字面量；解构/比较表达式 | 首批不新增放行；待荣耀语法补证后逐类设计 | 荣耀172项暂缓 |
| T14 | minified一行包含语法token字段和稍后出现的凭据；匹配在截断窗口外 | 凭据仍拒绝，检测不受报告截断影响 | 长行补证/后续设计 |
| T15 | 快照采集、恢复读入、publish候选扫描使用同一组安全/拒绝fixture | 三入口结论一致；拒绝发生在可上传快照创建、目标写入或Git/网络变更之前 | 集成 |
| T16 | 基础扫描器的Codex恢复配置/安全报告调用者 | 不能因profile误报修复弱化其他调用路径；执行相关既有回归 | M0调用范围 |
| T17 | 既有Git元数据排除、含.GiT旧快照拒绝、路径逃逸、junction、中文搬运/续传、冲突备份、双改保护 | 必须继续通过；不得通过修改其预期绕过保护 | 保持d076fff修复 |
| T18 | 若未来另行接受哈希绑定例外：文件/路径/跨度/规则/指纹变化；签审缺失；只在采集端例外但恢复/发布无例外 | 例外失效或显式拒绝，不能三入口不一致；不在首批实施 | M4未来选项 |

HP-03实施时应将M0红测放在最先；将各候选允许测试与相邻/回退拒绝测试成对增加。人工构造凭据通过字符串拼接等方式在测试运行时生成，测试日志不得输出值。没有新修复提交前，不声称上述测试通过。

验证顺序：新增红测 → 范围明确的最小修复 → 对应定向/拒绝测试 → 原定向与相关调用者回归 → npm test全套 → 两机同一新SHA的独立复验。固定源码SHA、原始文件与原验收报告不改。
