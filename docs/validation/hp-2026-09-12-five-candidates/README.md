# 惠普五处候选：两处形式实现，三处暂缓

交接输入 **d654053f50aeeb268d685eeda9f1233eaf6ae18f**；独立clone固定该HEAD，与已通过源码 **1ad915b9b9a45289dd9540408907ce593982e227** 的src/test/scripts/package及lock无差异。独立修复源码 **04672a6314d66c745bb11819e90f71b5ced0d3d0**。

HP-007/008对应TypeScript构造语句形式已实现，待荣耀独立复核；HP-001(PHP)、HP-004/005(Ruby)继续暂缓。原11处Python形式保持。**这不表示13处真实文件采集通过，也不表示HP-03整体或迁移完成。**

## 本机结果

| 检查 | 结果 / 退出码 |
|---|---|
| npm ci --ignore-scripts --no-audit --no-fund、基线build | 通过 / 0 |
| 修改前实际impact | HIGH，7上游符号、1直接调用者、3入口流程 / 0 |
| 初始红测 | 28项20通过8失败 / 1；其中1项断言契约纠正，原日志保留 |
| 正式红测（源码未改） | 28项21通过7预期失败 / 1 |
| 初步定向 | 118/118 / 0 |
| 增补拒绝测试后最终定向 | 121/121（原90 + 新31） / 0 |
| npm test，含pretest构建 | 539/539 / 0 |
| 刷新索引及提交前detect_changes | 退出0；详见detect-all.json |

[命令](commands.json)、[最终执行前环境](full-pre-env.json)、[正式红测](red-final.log)、[定向](focused-final.log)、[全量](full.log)、[候选逐项边界](candidate-scope.json)。每条命令在spawn前保存受控环境字段，显式delete顶层UAGENT；OPENCODE指向独立clone，测试可覆盖fixture。用户/Home/Codex/AppData/XDG/TMP/npm缓存及两个npm配置路径均隔离。

正式红测包括5个允许样本被扫描器拒绝、1个模板值拒绝断言失败，以及人工采集安全对照被拒绝；不是依赖或启动故障。带注释的允许样本基线已能通过，不冒称它是红测。初始测试错误要求解码JSON转义字符串内部源码，已改为要求该字符串保持不掩码；基础扫描器不是通用解码器。两个红态日志和本地测试副本都保留，无红态提交。

## 最小范围与拒绝保护

生产仅profile-expressions.ts：新增typescriptPlaceholders并在recognizeProfileExpressions处理候选。必须是.ts或闭合ts/typescript围栏中的顶层行首完整构造语句，const/client/new/Anthropic/单一apiKey字段/括号/分号精确匹配；单或双引号的精确占位符字面量才可掩码。名称限定只是缩小语法范围，不代表构造器来源可信。更名、附加字段、函数内、缺分号、尾接表达式、JS围栏/文件均不获新允许。

只掩码该字面量的UTF-16码元，注释、其他字段及原始换行不改。未识别的TS apiKey冒号形式增加保守拒绝，包含模板字符串、非空值前注释及跨行值；不是为其新增允许。普通引号转义字符串和模板中的示例不解释为源码。原基础规则、Python逻辑、M0和B1终止判断未改。

拒绝测试包含非空拼接/跨行逻辑/条件/属性/索引/类型断言/前后缀/大小写/未闭合/相邻字符串/插值，LF与CRLF、非BMP前缀、注释及邻接人工凭据行号。安全纯TS样本先用new Function仅解析、不执行。三入口对同一人工文件验证允许对照与拒绝：无采集产物、恢复目标字节及基线不变、publish无新增mock调用和.git；坏快照重新计算有效摘要以排除完整性错误。无真实Git/网络发布。

## 暂缓项与后续进入条件

PHP命名参数和Ruby关键字参数的局部历史形状已明确，但当前lexer不支持这些语言；直接加冒号分支不能证明heredoc/nowdoc、百分号字符串、语言专属注释、插值及续行边界。已有人工原形、拼接和嵌入示例拒绝回归，未声称这些语言完整覆盖。三处继续暂缓，不为凑五处扩大例外。

下一阶段需要专用的有界词法识别或可复核解析方案，先做上述语境区分红测及完整参数边界测试；不能简单把Ruby映射Python或PHP映射JS。荣耀本轮先独立复核TS两处实现及三处暂缓依据，若同意则返回明确PHP/Ruby下一轮范围；本轮不安装语言运行时或引入运行时解析依赖。

## 证据及剩余阻断

已读取荣耀README、REVIEW、commands、isolation、显式执行前环境与补充定向/全量结果，接受RY-09有限复核通过；旧474通过与漏洞失败及508通过证据原样保留。图索引存在预算截断/跨语言字段/大JSON跳过提示，不以缺边证明无影响；调用链仍为profile扫描到采集、readSnapshot恢复与publish。

M2、172项未知来源、RY-03来源版本绑定缺口均保留。未重搜或替换链接，未真实采集、上传个人快照、安装插件/Severin联动、真实恢复、私有往返、双向同步或大文件迁移。真实配置、记忆与原始材料未改，本轮不声称全机摘要审计。

荣耀请固定独立源码，显式隔离并记录执行前环境，构建、五文件定向与全量复验；分别给两处形式、三处暂缓、三入口保护结论。只更新荣耀负责的复核状态，推送脱敏报告、核实远端SHA，并返回可复制惠普提示词。
