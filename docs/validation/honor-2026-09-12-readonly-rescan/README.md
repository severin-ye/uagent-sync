# 荣耀 RY-04-PRE：两机只读对照一致，仍有阻断

独立clone固定 **af3f79c2c66a3d3c17fea20de0b12cc5577faf0f**；交接02c2a7d32bd5ea65b5afd3b0e9ed56d9c23efe68仅文档前进，受检源码无差异。

**13文件旧摘要全匹配、0漂移，前后13份摘要一致；5文件通过、8文件阻断，10次命中。** 与惠普逐文件状态、规则和原始行号一致。命令退出0表示检查完成，不表示全部安全；没有生成快照。

| 残留分类 | ID | 命中数 |
|---|---|---|
| PHP/Ruby复杂围栏 | HP-001/004/005 | 3 |
| M2参数说明 | HP-009/010/013/018 | 4 |
| 来源未确认 | HP-006 | 1 |
| 密码示例 | HP-022/023 | 2 |

规则为sensitive-assignment 9、known-token-prefix 1。Python11与TS2共13个旧候选不再命中；并非所有包含Python候选的文件都通过，部分仍受M2阻断。三处PHP/Ruby仍被完整围栏限制阻断。

[逐文件扫描](scan-result.json)、[两机对照](comparison.json)、[23项ID映射](candidate-mapping.json)、[汇总](RESULT.json)。每项候选仅因旧摘要一致才映射原行号；未上传实际内容或新的私有文件摘要。

[序列方案与先行拒绝测试](SEQUENCE-REVIEW.md)、[三个围栏位置核对](region-review.json)。建议下一轮惠普仅实现两种有序完整序列覆盖三个围栏，先红测再最小修复，不按行跳过、不支持任意导入或多语句。M2、HP-006、密码示例、172未知项和RY-03继续独立阻断。

[命令/退出码/执行前环境](commands.json)、[实际脚本](scan-script.txt)、[隔离wrapper](executed-wrapper.txt)。脚本只导入纯内容入口，完整UTF-8输入，路径固定为旧13文件；不递归扩扫、不导入采集/恢复/发布模块。npm仅用于独立clone依赖和构建，不是安装插件。固定源码未变，本轮未重复634全量回归，不将前轮结果计为本轮测试。

原始日志、绝对路径和文件摘要留荣耀TEMP；公开副本脱敏并规范换行与行尾空白，原始证据保留。两个读点一致仅代表这13文件，不能据此声明全机配置/记忆没有并发变化。未改真实文件、链接、配置、记忆或解析器；只更新荣耀状态。只读预检完成不等于HP-03、HP-04或环境迁移完成。
