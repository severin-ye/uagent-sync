# 惠普 HP-03-M2-PRE：四处参数说明的受限方案

**仅方案，M2仍阻断。** 两文件旧摘要与前后摘要全部一致，0漂移；HP-009/010/013/018均为函数首条文档字符串内的Args参数说明，不是help调用参数或真实赋值。没有新修复源码。

核对输入78e120b3367de2cd11f56faa1d52c05a5de160fa，src/test/scripts/package及lock与70428520228adcac3ac66a2cfb857667b7b69aed无差异。已读取荣耀sequence的README/REVIEW/RESULT/commands/scan-result/comparison/candidate-mapping，接受286/704及8通过5阻断7命中的有限复核；荣耀首轮空环境变量覆盖fixture失败与恢复证据保持原状，不计作本轮测试。

## 只读方法和明确事实

严格从旧hp-findings.json选四ID对应两文件，只读完整UTF-8文本，用Python3.11 ast.parse与tokenize取得语法及原始位置；不import/eval/exec受检代码，不调用网络或真实采集。每文件旧摘要及前后摘要匹配，后续细节读取也核对前后相同；此结论只覆盖两份受检文件读点，不声明全机无并发变化。绝对路径/原始值不进入公开报告。

| ID | 文件简名 | 命中行 | 完整STRING token起止（行,0起列；结束不含） | 函数行范围 | 描述组 | 建议仅掩码参数名区间 |
|---|---|---:|---|---|---|---|
| HP-009 | openalex_cli.py | 100 | (93,2)至(105,5) | 92–109 | D1 | 第100行[6,13) |
| HP-010 | openalex_cli.py | 125 | (120,2)至(133,5) | 112–171 | D2 | 第125行[6,13) |
| HP-013 | openalex_cli.py | 344 | (335,2)至(348,5) | 332–379 | D2 | 第344行[6,13) |
| HP-018 | openfda_query.py | 158 | (148,2)至(162,5) | 137–179 | D3 | 第158行[4,11) |

每处链为Constant(str)→Expr→FunctionDef→Module；均为函数体首条Expr。完整普通三引号字符串，无前缀、转义或插值；目标条目为Args段的单行api_key说明，匹配实际形参（HP-018是keyword-only参数，不能只检查位置参数）。D1/D2/D3分别24/36/17字符，D2两处逐字一致；仅公开组号、长度和结构，不发布说明原文或实际值。长度不是未来放行条件。

当前目标字符串没有调用、拼接、插值、布尔回退等求值表达式。函数签名与函数体另外存在默认值/Name等语法，但不属于说明字符串；这不证明整个函数无执行或无秘密，也不把运行时传入值判为安全。当前字符串完整性由语法树与token证据确认，不是因为出现Args/help就推断。

详见[structure.json](structure.json)、[最小方案](PLAN.md)、[先行测试设计](TEST-DESIGN.md)、[命令及退出码](commands.json)。本轮未实现识别器、未改原文、未新增运行红绿测试，未重跑构建/704全量或13文件扫描；这些是前轮结果，不冒充本轮执行。

## 分析脚本故障与恢复

首轮结构脚本结果文件与wrapper的同名命令元数据冲突，后续details出现KeyError；改用独立命令标签重新读取分析成功。跨度补充脚本把readline调用结果传给tokenize而非方法，出现TypeError；修正后spans-final退出0。两次属于只读分析脚本问题，不是源码/安全红测，失败及恢复日志全部保留；受检文件未写入。

## 明确结论与未知项

明确：四处是静态文档条目、两文件无漂移、属于三种固定说明形式；仍不能据此掩码整字符串或任意说明正文。建议仅在完整语法、Args结构、同名形参和逐字固定说明D1–D3全部通过后，掩码7字符参数名，冒号/正文/邻接文本保持原样。

未定：三种说明的本地语义复核与未来精确匹配表示、生产Python语法定位方式、未知/畸形语境的弃权粒度，必须由荣耀审阅PLAN/TEST-DESIGN后确定。Python AST仅用于本轮只读取证，不等于决定为生产引入Python运行依赖。未经审阅不得实现通用文档白名单。

下一轮请荣耀核对同四处摘要、语法/跨度及D组相等性，逐项接受或指出方案分歧，尤其完整函数文档字符串定位、精确正文限制、弃权粒度与无副作用门槛；本轮和下一轮方案复核均不放行M2。复核后再交惠普确定实施范围。

HP-006、HP-022/023、172未知来源、RY-03保持阻断，不重复搜索、不替换链接。未做真实采集、插件安装、恢复或快照发布，保留配置、记忆、原文及历史失败。仅更新惠普HP-03-M2-PRE，HP-03/HP-04/环境迁移不标完成。

公开inspect.py.txt将局部变量token更名为string_token，避免其赋值形状触发基础扫描器；仅为脱敏展示，实际执行原脚本保留本机。提交前detect_changes退出0，文档映射2符号、0流程；生产符号未修改，未运行符号修改影响分析。索引存在预算截断及大JSON跳过，缺边不作为安全证明。
