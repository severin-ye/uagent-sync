# 先行测试设计（未执行）

| ID | 人工样本与断言 | 基线预期分类 |
|---|---|---|
| T01 | 四处结构人工镜像、三D组、普通函数和keyword-only参数、原行号100/125/344/158；新规则允许且仅7字符掩码 | 应观察真实允许断言失败；不能以环境故障作红测 |
| T02 | 同条目正常说明与新增非空值/前后缀/第二赋值配对，D组同长度不同文本仍拒绝 | 拒绝回归或发现漏检才记新增红态 |
| T03 | 同行/前后行真实赋值的人工替身，全部键名别名/大小写、带引号字段、值前注释/括号/模板；文档内外均覆盖 | 独立记录每项拒绝，不预设基础扫描能覆盖 |
| T04 | 跨行加号/逻辑回退/非空默认/格式化/插值/隐式相邻字符串；闭引号后继续运算；空行后的缩进续写 | 不获M2掩码且危险组合拒绝 |
| T05 | 单/三引号未闭合、转义闭合符、raw/f/bytes字符串、嵌套伪文档/注释、未知模式后伪安全条目 | 文件本轮M2掩码为空，保留拒绝 |
| T06 | 非首条Expr、赋值字符串、help调用参数、模块/类文档、同名字段不在形参、错缩进/错Args层级 | 不认作文档候选；不按名字/路径白名单 |
| T07 | 缺函数冒号/括号、不合法签名、签名默认值携带人工凭据、动态表达式外部仍可被扫描 | 不执行代码，候选拒绝/弃权，外部秘密不被掩码 |
| T08 | LF/CRLF、非BMP前缀、UTF-8字节与Unicode/UTF-16索引转换、多文档字符串、不同缩进 | 精确全串对比仅7字符变化，原行号不变 |
| T09 | 说明正文里已知token/Bearer或第二敏感键值，正常参数名与实际值边界 | 所有原始前缀/Bearer拒绝保留，不掩码说明正文 |
| T10 | 两文件结构各做三入口安全允许/危险拒绝配对，逐项累计拒绝、无快照目录、目标字节/baseline不变、无新增mock Git调用 | 坏人工快照重算有效摘要；不得用摘要失败假冒扫描拒绝；不真实发布 |
| T11 | 现有M0、Python/TS/PHP/Ruby单语句/序列、续行、带引号键与注释保护；原始未确认/密码样本 | 原拒绝与允许回归保持，不删保护凑通过 |
| T12 | 未来固定修复源码后只读共同13文件，旧摘要/前后摘要核对，漂移独立列出 | 四M2是否清除须实测，其他三项继续阻断；不生成真实快照 |

所有“真实赋值/凭据”测试使用人工SYNTHETIC样本，不使用实际凭据。先保存实际红/绿计数及退出码，旧已拒绝用例只称回归。当前只提交设计，没有新增安全测试通过或失败的结论。
