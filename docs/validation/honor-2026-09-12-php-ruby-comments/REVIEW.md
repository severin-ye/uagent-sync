# RY-11 注释修复的独立审阅

固定对象 af3f79c2c66a3d3c17fea20de0b12cc5577faf0f；比较 e3153d9075bfc19219071e3cfd87a5f7ed2ab38f。交接027840fa91973c982bd423fa2e54f0c208868017包含修复，交接文档与固定对象src/test/scripts/package及lock无差异。

## 生产变更

src/lib/profile-php-ruby.ts:101 仅一处正则变更。旧注释检查大小写敏感并缺authorization/api-key等；新表达式为大小写无关的 api[_-]?key、token、secret、password、authorization，与secret-scan.ts:10的敏感赋值键集合一致。仍按赋值形状保守拒绝，不判断值是否实际凭据。

该变化只增加或保持拒绝命中，不改变匹配成功区域、token数量、构造语句、占位符或掩码代码。注释原文未改，行号仍由原始 comment.start + match.index 计算。因此单条完整构造形式的允许条件保持；多语句/import、标签切换、heredoc/nowdoc、百分号字符串、文档块、插值、转义、未闭合等原限制未扩大。本轮没有运行PHP/Ruby解释器，不能据此证明任意语言语法的完备识别。

## 测试与三入口审阅

新增36项键名测试：两语言各18种拼写，每项覆盖LF/CRLF与冒号/等号，共144个组合。每个组合同时检查普通注释安全对照、原始长度、注释尾部完整保留、sensitive-assignment@2。原39项词法与边界测试未删。新增2项三入口测试与荣耀原脚本保持同一保护结构。

三入口先完成安全人工fixture的采集/恢复允许配对，publish仅到mock Git门槛；再独立记录七项危险输入拒绝及无副作用断言。被篡改人工快照重算有效摘要，避免只因摘要不匹配而产生假阳性。拒绝时检查无新快照目录、恢复字节和baseline不变、mock调用保持1、无.git。原脚本另行运行，旧0/2证据仍在前轮目录，不覆盖。

## 证据边界与下一步

RY-PR-01的有限修复结论由本目录RESULT与实测日志给出。原危险尾部单独在旧版也漏检，组合被占位符阻断；本轮补的是该组合的注释拒绝，不宣称修复所有敏感内容识别问题。

HP-001/004/005仅有受限语法形式证据；本轮未读写真实Skill、采集快照、安装、恢复、链接或私有往返。M2、172项未知来源、RY-03、复杂代码区域、真实记忆变化来源与完整迁移继续未解决。

建议下一轮进入HP-04-PRE只读复扫阶段：固定同一源码，对惠普已有13份受检Skill逐文件重新运行纯内容扫描，先核对旧摘要并把有漂移项分开；输出文件数/命中数/行号/规则及既有候选ID映射，判断16个候选在完整文件语境中实际怎样。只读扫描不生成或发布快照，不安装或恢复，不修改原文来消除命中；未知来源继续拒绝。公开只回传脱敏结果和摘要核对结论，不上传原文/凭据。剩余修复范围应由实际残留命中确定，不先扩写多语言解析器。HP-03整体不因此勾选完成。


| 事项 | 现在是什么样 | 准备改成什么 | 依据和理由 |
|---|---|---|---|
| 惠普真实文件只读复扫 | 人工形式已修，完整文件效果未知 | 固定源码核对既有13文件与旧摘要，纯扫描输出残留统计 | 先用实际残留决定后续范围，避免无依据扩写解析器 |
| 未确认项 | M2、172项与链接来源缺证据 | 保留阻断，仅分类和提出最小必要输入 | 不把测试通过冒充安全来源或完整迁移 |
