# 惠普公开来源机制与实际文件副本集中验收

固定源码/接收交接：`1056d411419082bd18ad8f76fbb23720a3a3b93d`。独立源码目录完成本轮；9份公布的源码LF摘要全部一致。报告在独立交付工作树提交，只更新惠普状态，无新生产修复。

## 实际完成

- Nodev24.19.0满足新声明；隔离ci和构建退出0，两份profile-public-source测试13/13，完整npm test一次869/869、0失败/取消、退出0。全量包含打包消费者；未另跑旧M2实验或不受支持的Node18。
- 固定82项只读定位惠普原件：13存在且全部精确匹配，69缺失、0漂移、0链接跳过。只把存在文件复制到新受控目录，未扩扫其他目录。check使用新缓存从三个公开仓库独立获取固定blob，82项摘要全部核对；13份副本获精确匹配。check退出1是69项ENOENT，不是13份失败，也不是82份全绿。
- 使用实际发布脚本对该13份副本依次snapshot、restore-preview、restore-copy，三个命令退出0；快照13文件、0个.git，恢复到不存在的新目标home，13/13摘要一致，13份真实原件与副本再次核对全部未变。每次脚本使用新的缓存/报告，不借个人快照学习provider。
- 旧13文件/23候选映射已核对，历史摘要均与固定公开清单一致。HP-006、HP-022/023及M2四处所处的本机固定文件，现由公开来源精确复核通过；不是扩展旧M2解析器，更不放行这些路径未来修改的内容。69项不在惠普，不能算已迁移或把它们从清单隐去。

## 机制与失败拒绝审阅

安装代码旁固定清单，来源限定GitHub仓库URL、40位blob与64位SHA256；从独立bare/promisor仓库cat-file获取原始字节，只有清单标注crlf的对象作确定性LF→CRLF，再比对完整摘要。provider必须由可信宿主注入；默认扫描先执行，仅精确逻辑Skill路径和完整字节匹配解决命中。路径/内容改变、伪造context、重复/失败provider、已清单坏UTF8与未清单二进制边界均由本轮13项测试覆盖。三入口变更拒绝无副作用，发布允许也只有mock。

另用严格人工spawnSync替身测试实际adapter：下载失败立即抛错、错误blob字节触发摘要拒绝、已有缓存拒绝，共3项通过，失败阶段没有真实Git调用；未改清单或生产函数。随后从真实独立下载缓存重新cat-file核对82项完整摘要。真实Git仅下载公开源码；没有私人Git发布。

## 隔离与证据

用户/应用/Codex/XDG/TMP/npm配置/cache/prefix在Public受控目录；HOME和USERPROFILE一致，显式删除顶层UAGENT/OPENCODE workspace变量及NODE_PATH/NODE_OPTIONS，PATH使用明确当前Node。消费者祖先plugin/SDK检查另记录。构建/全量串行；真实文件读取仅限固定清单。所有副本、blob缓存和快照保留本机，不上传原文或模板。

logs/和commands.json保留实际命令、执行前环境、退出码；copy-inventory区分缺失与通过，candidate-mapping关联旧ID，roundtrip记录逐文件布尔校验，adapter-review记录来源核对与失败保护。公开只含路径脱敏、已公开逻辑路径、规则/行号及统计，不含快照manifest或文件正文。只在已验证清单范围内得出结论。

## 剩余与下一步

本机可做的当前源码验收、13份实际副本检查和往返全部完成，无新可修程序问题。69份荣耀文件确实不在惠普，本轮没有把荣耀的原件传来，故无法声称两机资料已交接。旧链接缺旧版本证据、npm事前状态及历史记忆写入归因仍缺可靠证据；本轮不重复搜索或猜测还原。

下一步应集中进入已核验资料的实际交接安排：须有私有/离线传输目的地、真实目标范围和备份回退位置，才能搬运荣耀文件、安装与运行验收及双向往返；不再重复本批无变化的程序检查。旧链接须旧版本证据或用户明确选择已核验替代归档。保护原配置、记忆与链接，未真实安装/注册/覆盖恢复/发布个人快照。HP-03、HP-04和完整迁移保持未完成；本机13份成功不等于完整Skill覆盖。
