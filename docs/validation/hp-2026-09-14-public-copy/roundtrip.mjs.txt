import {spawnSync} from 'node:child_process';import fs from 'node:fs';import path from 'node:path';import {createHash} from 'node:crypto';
const root='<USER_HOME>/hpr/src',base='<CONTROLLED_ROOT>',ev=base+'/e',node='C:/node.exe';
for(const mode of ['snapshot','restore-preview','restore-copy']){const home=mode==='snapshot'?base+'/copies':base+'/restored';const args=['scripts/verified-public-profile.mjs',mode,'--home',home,'--codex-home',home+'/.codex','--workspace-root',home+'/workspace','--cache',base+'/cache-'+mode,'--report',base+'/private-'+mode+'.json','--snapshot',base+'/snapshot'];const r=spawnSync(node,['<USER_HOME>/hpb/replay.mjs',root,ev,node,mode,JSON.stringify(args)],{encoding:'utf8',maxBuffer:2e6});console.log(r.stdout,r.stderr);if(r.status)process.exit(r.status);}
const hash=p=>createHash('sha256').update(fs.readFileSync(p)).digest('hex'),inventory=JSON.parse(fs.readFileSync('<USER_HOME>/hpr/copy-inventory.json'));const m=JSON.parse(fs.readFileSync(base+'/snapshot/manifest.json'));
const rows=inventory.rows.filter(r=>r.copied).map(r=>{const relative=r.source.replace(/^agents\//,'.agents/').replace(/^codex\//,'.codex/');return{source:r.source,restoredMatchesCopy:hash(base+'/restored/'+relative)===hash(base+'/copies/'+relative),originalStillMatchesCopy:hash('<USER_HOME>/'+relative)===hash(base+'/copies/'+relative)};});
const result={manifestFiles:m.files.length,gitMetadata:m.files.filter(f=>f.path.split('/').some(p=>p.toLowerCase()==='.git')).length,rows,missing:inventory.rows.filter(r=>r.status==='missing').length};fs.writeFileSync('<USER_HOME>/hpr/roundtrip.json',JSON.stringify(result,null,2));console.log(JSON.stringify(result));if(rows.some(r=>!r.restoredMatchesCopy||!r.originalStillMatchesCopy))process.exit(1);
