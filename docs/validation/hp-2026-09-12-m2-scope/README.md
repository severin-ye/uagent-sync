# 惠普 HP-03-M2-SCOPE：推荐范围，尚未授权实现

**推荐固定版本纯Node解析器进行严格整文件解析，并使用显式提供的私有精确模板。** 不使用Python作为生产运行依赖，不自写通用Python语法解析器，不公开D正文或短文本摘要。缺模板或无法证明语法边界时，M2掩码集合为空；四项继续按原扫描流程处理。

核对交接1803001ea50b0b5b5456c297ece60a62fc156645，读取荣耀m2-review的README/REVIEW/RESULT/structure/comparison/commands和共同清单。受检生产源码仍为70428520228adcac3ac66a2cfb857667b7b69aed，输入分支src/test/scripts/package及lock无差异。本轮不重复四处真实文件取证或172项分析，沿用荣耀已验证结构，明确不将旧摘要结果说成本轮新实测。

[生产语法合同](PARSER-SCOPE.md)、[私有精确模板合同](TEMPLATE-SCOPE.md)、[增补测试与进入条件](TEST-GATES.md)。只更新惠普任务，未改扫描器、依赖、真实配置或原文；未安装、构建、运行新红绿/全量测试、真实扫描、采集/恢复/快照发布。HP-03/HP-04及迁移未完成。

## 已收敛推荐

1. 解析器候选固定@lezer/python 1.1.18，使用完整输入和strict配置，拒绝错误恢复；所有候选缓存在内存，整文件与全部候选检查完成后一次返回。候选之后的错误必须撤销之前候选。
2. 只接纳顶层同步函数、块式函数体首条单一普通三双引号文档字符串、唯一Args节与本函数api_key形参。参数注解与默认表达式只按已解析子树界限跳读，不执行或将其内容掩码。
3. D组采用每设备显式指定的私有JSON模板，保存逐字正文和政策版本；运行时不得从受检文件/快照/环境同步内容自动提取。精确比较且无trim/casefold/Unicode规范化，缺失/版本不符时不放行。
4. 只替换参数名7个UTF-16单元，冒号、单空格、完整说明和其他内容保留。所有危险配对与旧M13保护仍须未来实测。

## 仍缺的最小输入

- 荣耀对具体语法节点/参数/Args归属/整文件失效合同的接受或具体修订。
- 后续明确允许依赖选型验证及加入包的实施任务；Node18、实际节点树、发布包与resolved依赖的兼容性尚未运行。没有engines限制不等于已经验证Node18。
- 后续明确私有模板由谁审核、在两机何处以显式路径提供；正文只能在本地私下核对，不需公开上传。当前没有创建或分发模板，用户此前禁止公开原文的边界保持。
- 范围收敛后独立的红测/实现启动指令。本报告不自动授权实现，也不要求为方案审阅先给实际凭据或公开正文。

## 外部资料证据与限制

使用agent-reach GitHub gh只读API核对上游固定tag与源文件；没有安装解析器。推荐包tag1.1.18指向091aab4b36d8e430053ee5603db170cd4eaec776；节点/exports见[上游](https://github.com/lezer-parser/python/tree/091aab4b36d8e430053ee5603db170cd4eaec776)。strict配置与抛错行为见[LR源码](https://github.com/lezer-parser/lr/blob/ed59b8b9c0c26164d6483f4c881a8c200184894e/src/parse.ts)。完整链接与查阅事实见SOURCES.json。未复制上游大段原文。

官网参考页读取403；一次请求不存在的src/index.js返回404，随后以官方grammar/package及lr/common源码核对。源文件检查支持API设计，不代表运行通过。lr/common资料来自记录的固定提交，未来lock解析版本必须重新核对一致性，不能用HEAD资料代替已锁依赖实测。

M2、HP-006、HP-022/023、172未知项和RY-03全部保留阻断。不重复来源搜索、不替换链接，保留配置、记忆和历史失败。下一轮荣耀只复核本范围和缺失输入，返回惠普明确的接受/分歧/是否具备另行启动条件，不先实施。

本轮source-check退出0，生产源码无差异；提交前detect_changes退出0，文档映射2符号、0执行流程。没有生产符号修改，不作实现影响分析。索引有预算截断与大文件跳过，缺边不等于无影响；暂存范围另由Git检查，仅本轮报告与惠普清单行。
