# 惠普 HP-03-M2-INTEGRATION-SCOPE

已形成生产接入范围提案，供荣耀复核；未实现或授权实现。核对交接d219ea66ca6c3b145b35124103dc30381341921e，生产源码仍70428520228adcac3ac66a2cfb857667b7b69aed，人工参考仍eaf93ff1c2f51038bb00a8a6c963727d386f3c26。

本轮读取荣耀指定材料及replay/logs共60份证据，核对生产及原型目录差异为空；用GitNexus query/context加源码逐段检查扫描和副作用顺序。未重跑82项或附加8项，未重做172项分析，没有新运行验收结果。荣耀通过仅作为既有人工证据引用。

## 推荐决定

1. 在profile专用层增加纯内容结果接口及M2上下文，基础scanForSecrets不改。M2只对.py整文件启用，绝不移植到Markdown围栏、JSON字符串或Codex MCP配置。
2. 新增异步操作工厂，由可信宿主注入人工provider；操作内加载一次并冻结，内部恢复规划复用，另一次预览/应用重新加载。保留现有同步函数和无模板默认行为，不把它们全部改成Promise。
3. 前缀/Bearer继续查看未被M13/M2掩码的文本，并保留现有两种精确ghp文档示例例外；M13拒绝行无条件保留。M2基于原文独立判断，只把7字符字段跨度应用到最终assignment扫描副本。
4. 默认CLI只用缺模板provider；未来人工测试可通过宿主工厂注入。真实模板提供者、私有路径和启用授权尚未确定，不增加环境变量/manifest配置入口来绕过它。
5. 三入口及Codex恢复调用者先做人工组合验收，再做生产包验证；格式不覆盖真实四处仍是明确缺口，不为通过真实扫描而扩大格式。

## 可复核材料

- [CALL-PATHS.md](CALL-PATHS.md)：当前实际调用、源码行号、扫描与写入边界。
- [INTERFACE.md](INTERFACE.md)：推荐接口、可信注入、加载次数、同步兼容和失败语义。
- [SCAN-ORDER.md](SCAN-ORDER.md)：M0/M13/M2组合及原始位置合同。
- [ACCEPTANCE.md](ACCEPTANCE.md)：人工允许/拒绝、无副作用和发布包门槛，全部待执行。
- [GATES.md](GATES.md)：文件范围、必要输入、分阶段进入条件和保守缺口。

本轮只有文档和证据，不改生产依赖/扫描器/入口或人工原型，不读取或准备真实模板，不重扫真实文件，不执行采集/安装/恢复/publish或链接操作。M2、HP-006、HP-022/023、172未知项、RY-03保持阻断，HP-03/HP-04及迁移未完成；只更新惠普行。

## 验证与记录限制

input-audit.json记录已读证据摘要及差异检查，graph.json记录当前独立克隆的查询。首次导航暂借旧同源码索引并收到过期提示，最终重新建立当前索引；未将旧索引当当前证据。第一次图结果文件与命令元数据同名导致被覆盖，改名后重取，最终采用graph-reviewed结果。辅助证据脚本首轮缺右括号退出1，修正后退出0；这不是安全红测。一次Windows rg通配路径查询失败后改用-g模式，不影响源码结论。所有这些都不记作程序测试失败或通过。

GitNexus索引有流程截断警告，不能把未显示边当作不存在；本报告的检查先后顺序以固定源码为准。detect_changes用于文档提交范围检查，不是新安全验收。公开日志只做路径脱敏、LF/行尾空白整理；真实配置、记忆及历史原件未改。
