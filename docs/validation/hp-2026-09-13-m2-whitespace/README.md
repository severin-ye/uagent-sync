# HP-03-M2-WS：RY-M2-01空白拒绝边界

交接d7fede87513d699a55a0b55f2b90b63982a4690f已核对。独立工作树固定此HEAD，src/test/package/lock与416120afbf2446463864e8292d1423e493055944无差异。本轮只修拒绝边界，不启用真实M2资料。

独立修复源码：`57b852f788e6ee4a9fcba7f0cda3c9f713a1e8ce`。当前Node全量852/852，退出0。代码与本报告分开提交，荣耀独立复核待执行。

## 红态与最小修复

荣耀三个原始重放文件按字节复制，SHA256一致，未改反例或预期。Node24.19.0和Node18.20.8各自重放：纯内容24检查中16通过/8失败，三入口6组中4通过/2失败。脚本子命令均退出1；wrapper退出0只是完成记录，不是验收通过。

首轮npm ci虽退出0，却缺少esbuild平台可选包；对应两Node重放与第一次新增测试只有环境错误，不算安全红态。明确--prefix及--include=optional的隔离ci重试后取得有效红态，所有记录保留。未改package.json/lock，不触碰用户目录npm事件现场。

生产只在scanProfileContent的M2专属try边界加入一项检查：出现空格、tab、CR、LF之外的ECMAScript空白或NEL（U+0085），即撤销整文件全部M2跨度。U+000C位于候选之前、之后、注释、参数默认值或字符串时均保守弃权；不尝试解释或归一化这些空白，不扩大语言格式。普通中文/非BMP字符不因非ASCII被拒绝。

缺模板和非.py继续旧行为；M13与基础扫描器不改，Prefix/Bearer仍检测原文，payload不改。弃权不等于凭空新增所有基础凭据检测：文件原有说明命中重新可见，因而组合危险文件被阻断；无原始发现的文件不能仅因零M2跨度就称凭据已拒绝。

根因仍分开：括号值单独的旧扫描盲点未在本轮改写；本次修复的是启用M2后组合文件从原说明阻断变为放行的缺口，并非掩码删除危险值。

## 验收范围

- 两Node原样人工脚本修复后各24/24与6/6，子命令退出0。人工危险输入无本次快照、目标或基线写入，publish新增外部替身调用为0；安全控制实际通过前三入口并到达第一个被拦截调用。没有真实Git/gh执行，源摘要保持一致。
- 原115项加33项空白回归，两Node各148/148。新增测试的最终先行红态33项中31个断言失败、2个空格/tab与None控制通过。早期同一行号测试因空数组直接取属性报TypeError，改成明确断言后重新取得31个有效断言失败，原记录保留。
- 完整当前Node npm test（含构建及既有打包烟测）的最终结果和代码SHA见RESULT.json。本轮不重复已经确认为旧问题的Node18全量19失败/4取消，不重做两类宿主消费者；这些限制没有被清零。
- 修改前影响分析CRITICAL，5个受影响符号、1个直接调用者，沿assertProfileContentSafe/字节入口到采集、恢复、发布。索引位于独立旧工作树，报告的3个文档提交落后已用生产差异为空核对；不会把图的截断信息当作无调用。提交前对实际修复工作树重新索引和detect_changes。

## 证据与复验

replay-results.json列两Node红绿计数、退出码和原脚本摘要；artificial/保留人工JSON、环境/子进程变量缺失记录及环境失败stderr。logs/与commands.json是本轮构建、测试和工具记录。公开仅人工内容、路径脱敏和统计，行末空白为展示清理；本机原始日志及人工产物保留，未上传快照或目标目录。honor-evidence-inventory.json记录读取的荣耀65份artificial/runtime证据摘要，历史819/819、115/115与新增漏洞失败记录不修改。

复验时固定RESULT.json中的独立源码，按荣耀原REPLAY复制artificial中的三个脚本到独立src的父目录（使用荣耀原文件），用新标签运行两个Node；再运行原八文件加test/profile-m2-whitespace.test.ts及npm test。依赖安装必须在已有源码manifest的明确--prefix内；环境变量需真实unset，禁止空字符串覆盖fixture。

## 剩余阻断

RY-M2-01仅惠普有限修复，待荣耀独立复核；不代改RY-17。M2真实四处、HP-006、HP-022/023、172未知来源、RY-03继续阻断。Node18历史全量兼容失败、omit-dev宿主依赖、惠普npm用户目录事件分别未解决，尤其不凭猜测还原旧文件。

没有读取真实模板、重扫真实文件、操作/重搜链接、真实采集、插件注册、恢复或快照发布。HP-03、HP-04与环境迁移仍未完成。

实际修复工作树detect_changes：2文件、4符号（1个生产函数及3个测试常量），28条受影响流程，CRITICAL；范围仍为已有纯内容扫描调用链。索引有流程截断，不能替代人工调用链与三入口验收。完整回归和定向结果均在最终生产改动上取得。

首轮环境故障的空JSON stdout以`.json.empty-stdout.txt`原空输出保留，映射见artificial/empty-stdout-records.json。
