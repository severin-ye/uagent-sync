# HP-02收敛范围及HP-03进入门槛

首批候选 **16 = 2环境空默认值 + 7参数引用 + 7精确API占位符**。4处M2参数说明首批暂缓；另2密码示例、1未确认字面量和荣耀额外172项仍阻断。按现有195个唯一项口径，未纳入首批候选的有179项；不能由此预测采集通过或实际修复数量。

## 共同约束

- 本轮禁止启动HP-03；下面是下一轮的执行边界，不是已经执行的结果。
- 实施前固定真实代码基线完整SHA，核对与已验证d076fff的源码差异。后续文档提交不是新代码验收SHA。独立目录、人工fixture，不读写真实配置/记忆/链接。
- 修改符号前完成上游影响分析。M0影响基础扫描器及Codex恢复调用者；M1/M3影响profile采集、恢复读入和publish候选。没有有效图索引时先在独立源码目录准备分析，不跳过必需检查。
- 不加目录/文件类型白名单，不改用户原文或manifest，不引入哈希绑定例外，不改变设备配置的模块化边界。
- 单规则同行一条finding的API可保留；要求拒绝结果、规则与原始行号一致，不要求报告API改为178个跨度输出。

## 阶段与门槛

| 阶段ID | 进入条件 | 工作范围 | 退出条件 |
|---|---|---|---|
| HP-03-M0-R | 下一轮明确启动M0；独立基线SHA和实际影响分析已记录 | 仅写M0失败复现及配对拒绝测试；包含标记同行泄漏、变体边界、行号 | 在未修复源码上观察到预期断言失败，保存测试名、命令、退出码；环境故障不能算红测 |
| HP-03-M0-F | M0-R确实复现 | 最小消除整行continue漏洞；仅局部识别完整占位符，不新增M1/M3允许形式 | 红测转绿，既有拒绝测试保持；没有额外源码范围变化 |
| HP-03-M0-V | M0-F完成 | 基础扫描器、profile三入口与Codex恢复调用者回归；npm test全套；提交前detect_changes | 所需检查通过，独立M0修复提交及完整SHA、脱敏日志、剩余阻断清单可审阅；未安装/恢复 |
| HP-03-M13-R | M0-V通过且M0独立提交可审阅；下一阶段范围仍限定M1/M3 | 仅新增完整引用、精确占位符允许场景和跨行/邻接拒绝场景 | 原M0基线上允许场景按预期红；拒绝场景不能因红测设计失真被删除 |
| HP-03-M13-F | M13-R完成，完整表达式识别边界已可说明 | 仅实现已确认16处对应形式；等长规范化保留换行；不支持的语法继续阻断 | 允许/拒绝成对测试绿；无法证明的候选退回阻断，不引入模糊豁免 |
| HP-03-M13-V | M13-F完成 | 三入口与相关调用者、d076fff原保护、完整npm test；提交范围检查 | 独立M1/M3提交及完整SHA、日志、实际覆盖/剩余项；之后才交HP-04/RY-04同SHA新快照复验 |

红测阶段若保留失败测试提交，不能将其作为可交付修复提交或违反项目“测试不过不提交”要求；以本地补丁/日志保存红态，正式修复提交前必须通过npm test。提交不能混入真实环境变动。两组修复独立提交，便于审阅和回退。

## M0边界

接受荣耀等长要求：若规范化替换文本，逐UTF-16码元保留长度、CR/LF及其他行分隔字符；不能把换行吞成一个字符。报告行定义沿用现有LF/CRLF规则，不擅自把字符串内Unicode字符算新行。

仅完整占位符值可被局部视为占位符；前后缀、相邻字面量、未闭合和大小写变体不获得额外豁免。同行其他敏感值全部继续检测。最小实现若无需掩码即可达成，应避免为形式一致性增加无用变换；任何采用的变换必须满足位置不变要求。

## M1边界

允许候选只限已观察到的Python环境读取空默认值和args.api_key完整表达式。不得只把前缀匹配替换掉：需要同时理解当前语法中的括号续行、反斜杠续行、运算符及表达式终结位置。无法完整确认时不规范化该表达式。

跨行or、空值合并、加号、条件表达式及非空回退必须作为拒绝场景设计。Python与JS分别用合法语法fixture；不能用Python无效语法的“??”假装已覆盖JS空值合并。JS/TS只是本轮必须保留的拒绝/兼容性场景，不因这些测试扩大允许集合。

加号仅是运算符，不能推断一定是字符串；但未知拼接/运算不是首批安全表达式。参数引用在别处被赋非占位符字面量时，那个位置仍应被扫描。不能因后续行没有命中就推定完整表达式安全。

## M2/M3/M4边界

M2明确推迟，T07/T08保留为未来设计，不允许偷偷在M1中连带忽略docstring。

M3仅敏感参数位置中精确、完整、带引号的your-api-key值；只有确认不是更大字符串/表达式的组成部分才可局部识别。多语言语法若不能安全限定则保持阻断，不按扩展名放行。

M4继续拒绝固定密码和未知字面量。对额外172项，147项具有报告中的RHS结构，25项未解析；这不是147项可放行、25项不可放行的划分，全部都不进入首批允许集合。

## 剩余事项不是核心政策分歧

1. M1/M3的识别实现与相邻/跨行边界尚需实际红测证明；这是实施门槛，不能以文档评审代替。
2. M2、未知fixture来源、打包源码映射、旧研究Skill来源仍未解决；保持阻断且独立跟踪，不阻塞M0独立修复的计划准备。
3. RY-03来源未确认，因此实际链接替换范围尚不能确定；不默认选择实体化或链接映射。
4. 此前两机419/419只证明d076fff回归通过；新代码、插件安装、真实资料恢复和跨机运行均需新的证据。
