# 测试设计修订（未实现、未运行）

继承前轮T00–T18标识，以下修订优先于[前轮设计](../hp-2026-09-11-scan-plan/TEST-DESIGN.md)。本轮不创建test文件、不运行红测、不修源码。

| 原ID | 接受的补充与预期 | 阶段 |
|---|---|---|
| T00/T01 | 完整占位符前后均放人工凭据样本；同规则同一行、不同规则同一行、多行各自出现。必须拒绝并保持原始规则/行号，不强制同规则同行多条finding。 | M0-R |
| T00/T10 | 未闭合、大小写变体、前后缀、相邻字符串、拼接不能令旁边的人工凭据消失；没有凭据的普通文本不因这个测试被武断判为密钥。 | M0-R及M13-R |
| T01/T04 | LF、CRLF、非BMP字符前缀、字符串内Unicode分隔符；安全表达式后另放确定命中的样本。检测报告行定义与原文一致；若有掩码，长度/换行完全保持。 | M0-R及M13-R |
| T02 | 单/双引号空默认值环境读取；完整单参数既有支持保持兼容。增加安全合法多行允许候选时必须先证明表达式边界。 | M13-R |
| T03/T05 | Python括号内及反斜杠续行的非空or回退、加号、条件表达式；JS合法的或/空值合并/加号/条件分支。不得只替换第一行安全前缀。 | M13-R |
| T04/T05 | args.api_key完整赋值/实参对照属性链、索引、调用、尾随运算符、相邻其他参数值；别处嵌入的凭据仍拒绝。 | M13-R |
| T06 | 未知标识符、运行时生成函数、AST属性访问、字符串字段名不能自动放行；生成函数名可被替换/遮蔽，测试目录也可能带真实值。 | M13-R范围控制 |
| T07/T08 | 暂缓允许设计；四处参数说明应维持当前阻断，不能通过忽略整个docstring消除。恢复此候选需另行明确引号配对、转义、上下文及同行其他值。 | 首批不实施允许逻辑 |
| T09/T10 | 敏感参数中的精确带引号占位符；Python隐式相邻字面量、各语言拼接、前后缀、跨行表达式、同行另一凭据均不获豁免。 | M13-R |
| T11/T12 | 固定密码示例、未知令牌、test/dist/.env.example人工凭据仍拒绝；在已审核文件末尾加值仍命中。 | M0/M13拒绝回归 |
| T13/T14 | 172项结构只可帮助构造拒绝或边界测试，不把分组当允许fixture。长行匹配在展示窗口外仍须拒绝；有多个匹配跨度不能仅看第一个。 | 保护边界，不扩大允许集 |
| T15/T16 | 采集、恢复读入、publish候选对同一原始fixture的允许/拒绝、规则、行号一致；基础扫描器的Codex恢复配置/安全报告调用者回归。拒绝前不创建可上传产物、不写目标、不改Git或访问远端。 | M0-V、M13-V |
| T17 | 保留Git元数据、旧快照、路径逃逸/junction、中文路径/续传、备份及双方修改冲突保护。 | M0-V、M13-V |
| T18 | 哈希例外仍为未来设计，首批不新增配置或机制。 | 不执行 |

所有凭据样本人工构造，不使用真实密钥；测试输出只含规则/行号和结果。每个允许fixture要有邻接、回退、拼接拒绝配对。未知语法允许保持阻断，不能为了达到16个候选全部通过而削弱拒绝测试。

失败复现必须在未修复的固定源码上出现预期断言失败；工具启动失败、编码错误或依赖缺失不是有效红测。各修复阶段正式提交前通过所需检查及npm test，记录新的完整源码SHA。上述设计不是本轮执行结果。
