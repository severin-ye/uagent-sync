# HP-02 方案收敛与分阶段交接

**HP-02 的首批方案已收敛，候选从20处缩为16处；HP-03尚未启动，扫描阻断和环境迁移均未完成。**

交接提交已核对：`98ceaa8de64b6110e29d157b04b95372535210d8`。两机此前验证源码保持 `d076fff50b142b4d09a2fbee9b0b8882b1554b96`。

本轮完整读取荣耀 [README](../honor-2026-09-11-plan-review/README.md)、[M0–M4意见](../honor-2026-09-11-plan-review/REVIEW.md)、additional-syntax.json、LINK-PLAN.md及analysis-checks.json。六个发布文件SHA256校验通过。逐项核对172个ID与前轮惠普映射的路径、行号、规则和文件摘要；核对178个跨度的坐标长度、唯一性及80个结构组的双向成员映射。

## 接受/异议结论

| 模块 | 惠普回应 | 收敛结果与依据 |
|---|---|---|
| M0 | 接受独立先行 | 同行标记不能掩盖其他凭据；若掩码，保持UTF-16长度和原有换行。不要求改变每规则每行一条的现有finding API。先独立红测、修复、回归，再进入M1/M3。 |
| M1 | 接受并加强边界 | 只含9处已观察到的环境/参数引用候选；必须确认完整表达式，包括合法跨行续接。未知尾部不按安全前缀放行；原始行号保持。 |
| M2 | 接受暂缓，撤回首批纳入建议 | 四处参数说明继续阻断；当前docstring引号和上下文识别的实施边界未充分证明，不为消除四处误报引入宽泛解析。 |
| M3 | 接受精确候选 | 仅7处敏感参数位置的精确、完整、带引号API占位符；拼接、前后缀及同行其他值不受豁免。 |
| M4 | 接受继续阻断 | 2处密码示例、1处未确认令牌字面量、荣耀额外172项不进入允许集合；哈希例外机制不进入首批。 |

补充澄清：所谓“不完整/变体标记不得豁免”，是不能借其跳过检测，不是要求把每段无凭据的普通文本新增判为凭据；拒绝测试应包含确实会命中的人工样本，避免无意义地扩大拒绝规则。

没有未解决的核心范围分歧。尚缺完整表达式/占位符识别实现的安全证明、未知项值来源、上游逐跨度映射及旧Skill来源等价性；这些作为进入门槛或继续阻断项，不用无限复核已一致的M0政策。若实施无法证明某个候选安全，则该候选保持阻断，不扩大豁免来凑满16处。

## 语法证据核对

| 维度 | 数量 / 判断 |
|---|---|
| ID / 文件 / 精确跨度 / 结构组 | 172 / 69 / 178 / 80，已核对发布数据内部一致性 |
| 已报告RHS结构 | 147项；仅语法已知，不是值来源已知 |
| RHS未解析 | 25项：19项位于字符串/模板内，6项其他未解析或非赋值 |
| fixture来源未确认 | 81项，与前述结构计数重叠，不可相加 |
| 已确认真实凭据 | 本证据没有建立任何确认；不证明不存在凭据 |
| 自动放行 | 0，所有172项保持阻断 |

详见 [evidence-checks.json](evidence-checks.json)、[逐ID复核](id-review.json)。惠普没有荣耀原始文件，不能独立重算原文正则、AST或来源真实性；本轮核验是对已脱敏发布数据和关联SHA的一致性核对。与原文匹配正确的结论仍引用荣耀证据。

RY-104四处跨度和RY-137一处跨度已补全，不再重复要求提供同一截断片段。gstack 1.68.3安装元数据、bundle/构建摘要一致性可作为本地构建证据；没有上游commit和逐跨度source-map，不能提升为上游来源验证或安全允许依据。函数名、随机生成调用、测试目录均不证明值为合成。

## 最新方案与后续执行边界

- [CURRENT-PLAN.md](CURRENT-PLAN.md)：取代前轮方案中首批20处的建议；历史报告保持原样。
- [TEST-DESIGN-V2.md](TEST-DESIGN-V2.md)：补充跨行边界、行号和拒绝测试，全部尚未实现/执行。
- [LINK-RESPONSE.md](LINK-RESPONSE.md)：接受来源核验优先及实体化副本候选；链接映射新功能排除在首批之外。

后续HP-03已分为M0红测→M0最小修复→M0回归，再到M1/M3红测→M1/M3最小修复→M1/M3回归。各阶段进入/退出条件见CURRENT-PLAN。**本轮仅完成计划分解，不进入任何阶段。**

本轮命令：只读Git拉取/祖先检查、JSON/哈希/映射核对（分析脚本退出0）、文档检查；未运行npm test、新失败测试、扫描修复、快照、安装、恢复或链接实验。未读取或写入真实配置/记忆内容，未修改原始验收证据和链接；不把此前真实摘要不变结论延伸成此次全机监测。

GitNexus detect_changes提交检测已尝试，退出1：独立checkout未注册索引（Repository not found）。图分析未通过；使用Git暂存路径限定本轮仅清单与新报告。代码阶段仍必须取得实际符号影响分析，不能沿用文档检查替代。

可复核命令（仓库根目录）：node docs/validation/hp-2026-09-12-plan-convergence/verify-evidence.mjs，退出0。该命令只读取公开证据并重生成本目录的两个JSON分析结果，不访问真实配置。
