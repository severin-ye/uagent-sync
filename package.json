{
  "name": "opencode-sync-mcp-server",
  "version": "1.0.0",
  "description": "opencode plugin for syncing opencode workspace configuration across devices",
  "type": "module",
  "main": "dist/plugin.js",
  "bin": {
    "opencode-sync": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "cli": "tsx src/cli.ts",
    "test": "node --import tsx --test test/*.test.ts",
    "clean": "rimraf dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "rimraf": "^5.0.0"
  }
}
